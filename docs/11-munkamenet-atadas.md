# Munkamenet-átadás — 2026-09-21

Ez a fájl a következő fejlesztőnek/agentnek adja át a **jelenlegi** állapotot. A részletes mérési előzmények a [10-bemutato-terkep.md](10-bemutato-terkep.md) fájlban, az állandó projektkontextus a repó gyökerében lévő `.ai-context.md` fájlban vannak. Jelentős feladat végén frissítsd ezt a naplót, hogy a folytatás a Git munkakönyvtárból is érthető legyen.

## A két weboldal szerepe

| Cél | Szerkesztendő fájl | Helyi URL |
| --- | --- | --- |
| Kutatók Éjszakája bemutató, most ez a fő cél | `scripts/control_panel.html` | `http://127.0.0.1:8902/scripts/control_panel.html` |
| Részletes műszaki szenzornézet, későbbi fejlesztés | `scripts/dashboard.html` | `http://127.0.0.1:8902/scripts/dashboard.html` |

A bemutatóoldal a műszaki oldal `?embed=1` módjából ágyazza be a C70 kamerát és a valódi `/map` térképet. A gyökérben lévő azonos nevű HTML-fájlok csak átirányítók. A roboton lévő `:8901` oldal korábbi telepítés, a Git-változások oda nem kerülnek fel automatikusan. A laptopos indító `scripts/start-demo-view.ps1`; a `-DashboardOnly` kapcsoló a műszaki oldalt nyitja.

## Mostani állapot és a legutóbbi újraindítás

- A felhasználó újraindította a robotot. SSH-val ellenőrizve a `pickerbot-bringup`, `pickerbot-rosbridge` és `pickerbot-webui` aktív lett; a `/map` publikálója a `slam_gmapping` volt.
- A C70 a reboot után `/dev/RgbCam -> /dev/video0` eszközként jelent meg, de a `pickerbot-c70` konténer újraindulási hibában volt, a `/usb_cam/image_raw` témának nem volt publikálója. A repó `docker/sensors/recover-c70.sh` szkriptjét a roboton futtattuk. Sikeresen visszaadott friss ROS-képkockát, 0 kilépési kóddal. A többi konténerhez és a hostszolgáltatásokhoz nem nyúltunk; mozgásparancs nem ment ki.
- A felhasználó a képet és a térképet látja. A nyers ROS-kép kezdetben 1–1,5 másodpercenként cserélődött, de a mozgás **15 másodperc késéssel** látszott. A képkocka időbélyegével a böngészőben 16,9, később 31 másodperc késést mértünk. Egy nyers rosbridge-kocka kb. 1,23 MB; a ritkítás önmagában nem oldotta meg a torlódást. A `/usb_cam/camera_info` ugyanakkor 0,2–0,4 másodperc késéssel érkezett, tehát a kameraforrás friss volt.
- A helyi SSH-kulcsot ez az agent-fiók `Permission denied` miatt nem olvassa, bár a fájl létezik. A dokumentált jelszóval a már telepített PuTTY `plink.exe` sikeresen kapcsolódott; a robot hostkulcsának ujjlenyomatát a helyi `known_hosts` alapján rögzítettük. A jelszó csak átmeneti helyi fájlba került, amelyet a kapcsolat létrejötte után töröltünk. Az indító most ugyanezt a Plink-utat használja tartalékként, ha a privát kulcs nem olvasható. A jelenlegi `:8080` alagút Plinkkel fut; a helyi `:8902` szerver továbbra is a repó aktuális oldalát szolgálja ki.
- A korábbi teljes méretű MJPEG-folyam régi kockákat halmozott: 12 másodperc alatt csak 3 kocka érkezett, a harmadik már 6,8 másodperces volt. A `scripts/dashboard.html` C70 URL-je **320×240 méretet kér, JPEG-minőség 55-tel**, de a robot jelenlegi web_video_server változata a méretkérést figyelmen kívül hagyja: a mért kép valójában **640×480**. Kábelen ezzel 5 másodperc alatt 148 kocka érkezett, a továbbítási késés legfeljebb kb. 0,1 másodperc volt. A felhasználó külön, kézmozdulattal **1 másodperc alatti** látható késést igazolt. Az oldal `Kamera: élő MJPEG` állapotot és friss 384×384-es `/map` térképet mutatott.
- A bemutató beágyazott nézete MJPEG-kieséskor nem iratkozik fel a torlódó nyers ROS-képre. Kapcsolathibát jelez, 8 másodpercenként valódi JPEG-pillanatképpel (15 másodperces határidővel) vizsgálja a kamera és az alagút elérhetőségét, majd siker esetén csak a beágyazott szenzornézetet tölti újra. A külön műszaki szenzornézetben a nyers ROS-tartalék és a kocka mért késése megmaradt. Ez a változás a térkép és az alvázvezérlés rosbridge-kapcsolatát védi.
- Kontrollált helyi kiesési próba: a Plink-alagutat leállítottuk; a bemutatóoldal kapcsolathibát jelzett, a `/map` közben tovább frissült. Az indító `-NoBrowser` újrafuttatása új Plink-alagutat nyitott. Az első változatnál kézi oldalfrissítés kellett a képhez; a pillanatképes visszatérés után ugyanebben a próbában **kézi frissítés nélkül** újra `Kamera: élő MJPEG` és friss 384×384-es térkép jelent meg. Az indító meglévő Plink-folyamattal végzett ismételt futtatása és a teljes Plink-újraindítás is sikerült. A jelszót tartalmazó átmeneti fájl nem maradt meg.

## Gemini változásainak ellenőrzése

A helyi `main` közelmúltbeli commitjai: `392e363` (korábbi funkciók átvitele) és `a9f3197` (indító újraindítása). A Gemini után ellenőrzött változás az `a9f3197`; a `main` jelenleg egy committal az `origin/main` előtt van. A felhasználó kezeli a commitot és a push-t. Az `a9f3197` javítása újrafuttatáskor helyreállítást célzott, de részleges kieséskor a működő helyi szervert is `Stop-Process -Force` hívással leállította. Ezt módosítottuk: a futó oldal megmarad, csak a hiányzó folyamat indul újra, és sikertelen új folyamat esetén csak azt állítjuk le. Az indító **újrafuttatásra javít**, nem önálló háttérfelügyelet; automatikus SSH-újracsatlakozás nincs igazolva.

## Biztonság és következő munka

- A kar fel-le ízülete a felhasználó szerint darál és sípol. A webes karpanel továbbra is kizárólag szimuláció. Élő karpróba csak áramtalanított fizikai ellenőrzés után.
- Az alváz webes vezérlése élesítés után valódi `/cmd_vel` parancsot küld. A webes megállítás hálózatfüggő; mozgáspróbához fizikai leállító kell. Ebben a munkamenetben nem élesítettük és nem mozgattuk a robotot.
- A bemutatóoldal finomítása az elsődleges feladat. A teljes műszaki szenzornézet bővítése későbbi szakasz.
- Következő mérés: az alagút hosszabb idejű tartóssága; a térkép frissessége mozgás alatt csak felügyelt, fizikai leállítóval végzett próba során. A C70 USB-leválás okát (kábel/hub/táp) külön vizsgáld. Az indító újrafuttatásra javít, háttérben automatikus SSH-újracsatlakozás még nincs. A jelenlegi oldalt egyelőre a laptop `:8902` portján nyisd; a robot `:8901` oldalát nem frissítettük.

## Helyi változások és ellenőrzés

A mostani, felhasználó által még commitolandó változások: `scripts/start-demo-view.ps1`, `scripts/stop-demo-view.ps1`, a két `scripts/*.html` oldal, ez az átadási napló és kapcsolódó dokumentáció. Ellenőrizve: PowerShell parser hibamentes; a két HTML inline JavaScriptje `node --check` alatt hibamentes; `git diff --check` hibamentes (csak Git sorvégjel-figyelmeztetés); a 25 meglévő Python-teszt átment közvetlen függvényhívással, mert ebben a helyi Pythonban nincs telepített `pytest`. A Plink-alagút és az élő MJPEG-folyam kábelen mérve működött. Az indító meglévő Plink-folyamatot felismerő újrafuttatása, a jelszavas teljes Plink-újraindítás és a böngésző automatikus kamera-visszatérése átment. Ne commitolj vagy pusholj a felhasználó helyett.

## 2026-09-21: térkép törlése és USB Wi-Fi

- A bemutató és a műszaki oldal térképpaneljén van **Térkép újrakezdése** gomb. A `scripts/demo_server.py` csak a laptop `127.0.0.1:8902` címén fogadja a helyi POST-kérést, ellenőrzi az Origin/Host/action mezőket, és csak a `pickerbot-slam` konténert indítja újra. Az addigi térképkép eltűnik, az új `/map` adatból rajzolódik újra. A felhasználó a gomb működését élőben megerősítette. A szerver a jelszót a meglévő README-ből olvassa, átmeneti helyi fájlt használ a Plinkhez, majd törli. Ne add ki a helyi szervert a hálózatra. A robot régi `:8901` oldalán a gomb nincs telepítve.
- A TP-Link TL-WN823N / RTL8192EU stick `wlan1` eszközként működik. A roboton a `pickerbot-robot-wifi` NetworkManager-profil automatikusan kapcsolódik a `TP-Link_A426` SSID-hez, címe `192.168.123.51/24`; a Wi-Fi jelszó csak a robot root-jogú, `0600` módú profiljában van. A `scripts/robot-wifi-failover.sh` telepített példánya `/etc/NetworkManager/dispatcher.d/90-pickerbot-wifi-failover`; Ethernet kiesésekor a ROS által használt `.50/32` címet a Wi-Fi-re teszi, visszatéréskor leveszi. A robot saját `wlan0` hotspotját nem módosítottuk.
- **Kontrollált próba, fizikailag bent hagyott kábellel:** az `eth0` kapcsolatot logikailag lekapcsoltuk, és 180 másodperces automatikus visszakapcsolást állítottunk be. A `.50` Wi-Fi aliasról SSH, a 9090-es rosbridge és a 8901-es webport elérhető volt, az útvonal `wlan1`-et mutatott. A roboton a `/map` és C70 publikálók futottak. A böngészőben a térkép az újrakapcsolás után ismét megjelent, de az MJPEG-kamera Wi-Fi-n ismételten megszakadt, és a pillanatkép-kérés több másodpercig tarthatott. A rosbridge szolgáltatást és a web-video konténert egyszer újraindítottuk; a SLAM-et, C70-et, LiDAR-t, bringupot és a kart nem indítottuk újra. A próba után az Ethernet automatikusan visszaállt, a bemutatóoldal ismét `Kamera: élő MJPEG` állapotot és friss, 384×384-es térképet mutatott. A Wi-Fi átállás így még **nem kész bemutatóüzemre**; valódi kábelkihúzást és újraindítást kábel nélkül nem igazoltunk.
- A kezdeti Wi-Fi jel körülbelül −78 dBm, a feltöltési link 6,5 Mbit/s volt. A felhasználó kb. 50 cm-rel közelebb vitte az eszközöket; ekkor −36 dBm és 39 Mbit/s volt mérhető, de a böngészős stream így sem bizonyult stabilnak az átváltás során. A bemutató helyén, végleges távolságon új mérés kell. A robot web_video_serverének 640×480-as tényleges képe a szűk Wi-Fi linken torlódhat; a méretcsökkentéshez szerveroldali megoldás kell, az URL-paraméter önmagában nem elég.

**Folytatás:** mérd a kamera HTTP MJPEG-folyamának képkockaidejét és méretét Wi-Fi-n; ellenőrizd, hogy az SSH-alagút hálózatváltás után új kapcsolatot nyit-e. Ha szükséges, készíts a roboton külön, kisebb felbontású/képkockasebességű bemutatófolyamot, a meglévő ROS-témát érintetlenül hagyva. Próbáld ki a tényleges kábel nélküli indítást csak visszaállítási tervvel és a bemutatóhelyen. A karral továbbra se végezz mozgáspróbát a fizikai hiba felderítéséig.
### Második Wi-Fi próba és helyreállítás, 2026-09-21 12:00

A második, 180 másodperces Ethernet-visszakapcsolással védett próbában a Wi-Fi-re új SSH-videóalagutat akartunk nyitni. Ez nem indult el. Utólag a NetworkManager naplója megmutatta, hogy a `wlan1` **már 11:49:34-kor**, a próba előtt elvesztette az AP-kapcsolatot; 11:49:50-kor `ssid-not-found` hibára váltott. Emiatt az Ethernet logikai lekapcsolása után sem a `.50`, sem a `.51` cím 22/9090 portja nem volt elérhető. Az időzített `nmcli device connect eth0` 11:56-kor visszaállította az Ethernetet. A `wlan1` azóta `disconnected` állapotú; a Wi-Fi-keresés üres listát adott, a célzott `nmcli connection up pickerbot-robot-wifi ifname wlan1` szintén „The Wi-Fi network could not be found” hibával zárult. Az USB-s eszköz (`2357:0109`) jelen van, a rádió nincs blokkolva. A kernelnaplóban 11:49-kor AP-hitelesítési időtúllépés és `Unhandled C2H event` üzenetek vannak. Az ok (jel/AP, stick, USB-ellátás vagy `rtl8xxxu` driver) nincs bizonyítva; jelenleg a robot **nem használható kábel nélkül**. További Ethernet-lekapcsolást csak akkor végezz, ha a `wlan1` előbb újra csatlakozott és a `.51` SSH-portja elérhető. Szükség esetén fizikailag ellenőrizd/dugd újra a sticket, majd nézd meg az AP láthatóságát és a router naplóját.

A hálózatváltás után a vezetékes böngészőoldal képe és térképe átmenetileg nem tért vissza. A `pickerbot-rosbridge` szolgáltatás és a `pickerbot-web-video` konténer újraindítása, majd az oldal frissítése visszaadta az élő MJPEG képet. A LiDAR `/scan` kb. 12 Hz-cel futott, de a SLAM `/map` nem adott új üzenetet; ezért a `pickerbot-slam` konténert újraindítottuk. Ez törölte az addig épített belső térképet, az új térkép viszont megjelent és ismét üzeneteket küldött. A végső böngészőállapot: `Kamera: élő MJPEG`, `Térkép: üzenetek érkeznek`, 384×384. Mozgásparancsot nem adtunk. A helyi indító fut, az aktuális bemutatóoldal `http://127.0.0.1:8902/scripts/control_panel.html`.

## 2026-09-21: a beépített Wi-Fi lett az elsődleges vezeték nélküli út

- A felhasználó kihúzta és visszadugta az USB Wi-Fi sticket. Ezután a `wlan1` ismét kapcsolódott, de a mért jel −82 dBm volt. A robot saját rádiója a beépített Intel 8265 (`wlan0`); korábban a `WHEELTEC_OrinSuper_Noetic_JP515` hotspotot sugározta.
- Az Ethernet megtartása mellett a robot védett USB Wi-Fi profiljából átvett jelszóval létrehoztuk a `pickerbot-onboard-wifi` kliensprofilt a `TP-Link_A426` hálózathoz. A beépített Wi-Fi címe `192.168.123.52/24`, útvonal-metrikája 150; az USB profil `.51/24` címe és 200-as metrikája tartalékként megmaradt. A jelszót nem írtuk a repóba. A beépített kapcsolat első jele −21 dBm volt az akkori közeli elhelyezésben; ez nem a bemutatóhelyen mért adat.
- A `scripts/robot-wifi-failover.sh` most Ethernet kiesésekor először a beépített `wlan0`-ra, ennek hiányában a `wlan1`-re helyezi a ROS `192.168.123.50/32` alias címét. A szkriptet a robot `/etc/NetworkManager/dispatcher.d/90-pickerbot-wifi-failover` helyére telepítettük, szintaxisát és vezetékes ágát ellenőriztük.
- **Kontrollált próba:** 120 másodperces automatikus Ethernet-visszakapcsolást állítottunk be, majd logikailag lekapcsoltuk az `eth0`-t. Az USB Wi-Fi ekkor `DOWN` volt. A `.50` alias a `wlan0`-n jelent meg, az SSH a robot megszokott `.50` címén működött, a laptop felé az útvonal `dev wlan0` volt. A laptopos bemutatóoldal ekkor `Kamera: élő MJPEG` és változó 384×384-es térkép állapotot mutatott. Mozgásparancs nem ment ki. Az időzített Ethernet-visszakapcsolás után a `.50` visszakerült kizárólag az `eth0`-ra, a `wlan0` `.52` címmel csatlakozva maradt. A régi hotspot-visszaállító időzítőt a sikeres próba után leállítottuk.
- A `pickerbot-onboard-wifi` automatikus kapcsolódása be van kapcsolva; a régi hotspot profilját megőriztük, de az automatikus indulását kikapcsoltuk. A két mód ugyanazt a rádiót használja, így most a robot saját hotspotja nem sugároz. Helyben `sudo nmcli connection up WHEELTEC_OrinSuper_Noetic_JP515 ifname wlan0` paranccsal visszaállítható, de ez megszünteti a TP-Link klienskapcsolatot. A dokumentált hozzáférés és ellenőrzés a [hálózati leírásban](09-robot-halozat.md) van.

**Még ellenőrizendő:** tényleges fizikai kábelkihúzás, indítás kábel nélkül, tartósság és kamerakésés a bemutató tervezett távolságán. A kontrollált próba a hálózati útvonalat és a két élő szenzornézetet igazolta; a bemutatóhelyen szükség lehet további jel- vagy videófolyam-optimalizálásra. A robotkarral továbbra se végezz élő mozgáspróbát a fizikai hiba kivizsgálásáig.

**Hosszabb utóellenőrzés:** az Ethernet visszatérése után a kamera élő maradt, de a `/map` nem adott új üzenetet egy 8 másodperces közvetlen ROS-mérésben, és a bemutatóoldal 15 másodperces frissességi figyelmeztetést mutatott. A `pickerbot-slam` újraindítása után is megismétlődött ez az állapot. Ez külön **SLAM/TF vagy álló robot melletti publikálási kérdés**, nem bizonyított Wi-Fi-hiba; a pillanatnyi kép látható, de az új térképüzenetek tartóssága nem igazolt. Következő lépésként felügyelt, fizikai leállítóval végzett mozgás közben hasonlítsd össze a `/scan`, `/tf`, `/map` időbélyegeit és a gmapping naplóját. A kart ne mozgasd. A beépített Wi-Fi a későbbi ellenőrzéskor is csatlakozott a TP-Linkhez, `wlan0` jelerőssége −27 dBm és adási linkje 300 Mbit/s volt; az Etherneten a `.50`, a beépített Wi-Fi-n a `.52` cím maradt.

### Fizikailag kihúzott USB-stick próbája

A felhasználó kihúzta a TP-Link USB Wi-Fi sticket. A roboton a `wlan1` eltűnt az eszközlistából; a beépített `wlan0` továbbra is a `TP-Link_A426` hálózaton maradt, a méréskor −33 dBm jellel és 300 Mbit/s adási linkkel. Az Ethernetet újabb 120 másodperces automatikus visszakapcsolással védett próbában logikailag lekapcsoltuk. A megszokott `.50` alias a `wlan0`-n jelent meg, az SSH és a bringup/rosbridge aktív volt, a laptop felé az útvonal `dev wlan0` lett. A videóalagút indítójának újrafuttatása után a C70 pillanatkép HTTP 200 választ adott **0,64 másodperc** alatt (20 439 bájt), és a bemutatóoldal élő MJPEG-képet, valamint változó tartalmú 384×384-es térképet jelzett. Az automatikus Ethernet-visszakapcsolás ezután lefutott: a `.50` cím kizárólag az `eth0`-n volt, a `wlan0` `.52` címen kapcsolódva maradt, az oldalon a kamera és a változó térkép továbbra is látszott. A mérés az adott helyen, rövid ideig tartott; a hosszú távú kamera- és térképfrissülést külön kell ellenőrizni. Valódi fizikai Ethernet-kábel kihúzása továbbra sem történt meg.

## 2026-09-21 (délután): kábel nélküli próba a felhasználó megfigyelése alapján

**Mit igazoltunk (a felhasználó szerint, számszerű mérés nélkül):** a felhasználó kihúzta az Ethernet-kábelt, és a robotot a beépített Wi-Fin (`wlan0`, TP-Link_A426) használva körbevitte a szobában. A bemutatóoldal működött, a rosbridge kapcsolat, a kamera és a térkép megjelent. Ez az első fizikai kábelkihúzásos próba. Kábel nélküli **újraindítást** nem próbáltunk.

**Problémák:**
- A kamerakép 1–2 percenként megszakadt. Az alagút (`start-demo-view.ps1`) újrafuttatása után egy ideig ismét működött. Egy alkalommal a felhasználó szerint a kép kb. 3 perc után magától tért vissza. Ez inkább a Wi-Fi-kapcsolat megakadására utal, mint az SSH-folyamat megszűnésére (az `ssh` 30 másodperc után feladja), de az ok **nincs bizonyítva**.
- A kamera késése kábel nélkül a felhasználó szerint túl nagy egy izgalmas bemutatóhoz. Nincs számszerű mérés; a korábbi ismert ok, hogy a robot `web_video_server` a 320×240 URL-kérés ellenére 640×480 képet küld.

**Mit változtattunk (a laptopon, a roboton semmit):**
- `scripts/start-demo-view.ps1` és `scripts/stop-demo-view.ps1`: UTF-8 BOM. Nélküle a Windows PowerShell 5.1 az ékezetes szöveget ANSI-ként olvasta, és szintaxishibával leállt.
- Új `scripts/watch-demo-view.ps1`: 10 másodpercenként egy JPEG-pillanatképet kér az alagúton át, két sikertelen kör után lefuttatja a `start-demo-view.ps1 -NoBrowser` parancsot. Naplót ír a `%TEMP%\pickerbot-demo-watch.csv` fájlba (idő, siker, válaszidő másodpercben, méret bájtban, újraindítás). **Szintaxisát és működését még nem futtattuk le**, itt nincs PowerShell.

**Következő lépések:** futtasd a `watch-demo-view.ps1`-et kábel nélkül, és a napló megmutatja a kiesések gyakoriságát és a válaszidőt. Olvasó mérések a roboton (`iw dev wlan0 link`, `iw dev wlan0 get power_save`) és laptopról `ping -n 60 192.168.123.50`. Ezek után dönthető el, hogy a kamera forrását kisebb felbontásra/kevesebb képkockára kell-e állítani (`pickerbot-c70` konténer újra létrehozása, robotoldali változtatás, külön jóváhagyással).

### Alagút-őrző mérése kábel nélkül, 2026-09-21 13:43–13:48

**Mérve** (`scripts/watch-demo-view.ps1`, 10 másodperces JPEG-pillanatkép az alagúton át, kb. 5 perc): 30 kérésből 30 sikerült, automatikus újraindítás nem történt. A válaszidő mediánja 0,17 s, két kiugró érték 3,62 s és 2,88 s volt (13:44:38 és 13:44:51), a többi 0,07–0,69 s. A kép mérete 12–23 kB. A felhasználó szerint a kamera ezúttal gyors volt, és nem szakadt meg. A pillanatkép egyetlen kép lekérése, ezért **nem** a folyamatos MJPEG-videó késését méri.

**Bizonytalan:** az előző kör 1–2 percenkénti megszakadásai ebben a mérésben nem ismétlődtek meg; az okuk (helyváltoztatás, Wi-Fi-jel, roaming, az akkori terhelés) nincs kiderítve. A két kb. 3 másodperces megakadás a Wi-Fi rövid kiesésére utalhat, ez sincs bizonyítva. A mérés egy szobában, egyetlen 5 perces futásból származik.

**Térkép:** az 5 perces körözés alatt a `/map` egy idő után szétcsúszott (egymásra fordult, elcsúszott falak). A körülmények, főleg hogy a robotot kézzel vitték-e (ilyenkor a kerékodometria nem mozdul), vagy hajtották, nincsenek rögzítve. Ez a korábbi, nyitott SLAM/TF-kérdés része; a szétcsúszás oka nem bizonyított.

### Térkép-elcsúszás vizsgálata és robotoldali mérés, 2026-09-21 14:10–14:20

A felhasználó joystickkel vitte a robotot (a joystick közvetlenül az alsó vezérlőhöz kapcsolódik, a webes sebességplafon nem érvényes rá), és gyors fordulások közben a `/map` szétcsúszott (egymásra fordult, elcsúszott falak). A `scripts/robot-slam-diag.ps1` csak olvasó módon mért a roboton, álló helyzetben.

**Mért:**
- `/scan` 12,0 Hz (szórás 0,0002 s), `/odom` 20,0 Hz, `/imu` 20,0 Hz, `/tf` kb. 100 Hz. A gmapping bemenetei nem estek ki.
- `/map` 10 másodperces mérésben 0,44–1,6 Hz, legfeljebb 3,1 másodperces szünetekkel. A `temporalUpdate` értéke 1 s, így álló robotnál ez várható; a mérés idején mozgás nem volt.
- `wlan0`: −28 dBm, 270–300 Mbit/s, **Power save be van kapcsolva**. A hatása nincs mérve, a beállítást nem módosítottuk.
- SLAM-napló (utolsó ~150 sor, álló robotnál): az átlagos illesztési pontszám kb. 1430–1460, `neff` 6,5 körül (8 részecskéből), a pozíció gyakorlatilag nem mozdul (`ad` ~1e-9). Az álló állapot tehát stabil. A CPU-terhelés (`docker stats`) sora nem került elő.

**Nem mért / bizonytalan:** a szétcsúszás mozgás közben történt, de mozgás közbeni SLAM-napló és CPU-terhelés nincs. Nem tudjuk, hogy a gmapping fordulás közben lemarad-e (8 részecske, 12 Hz-es scanek), vagy a kerékodometria csúszása okozza-e (mecanum). A `docker/slam/gmapping-tuned.launch` (20 részecske, nagyobb forgási zaj, minimumScore 50) egy **kipróbálatlan** ötlet, még nincs telepítve; a robot konténere a régi `gmapping.launch`-ot használja.

**Következő lépés:** mozgás közbeni mérés. Új térképpel indítva (a `pickerbot-slam` újraindítása törli a térképet), felügyelt, lassú, majd gyorsabb joystickos fordulás közben és közvetlenül utána: `sudo docker stats --no-stream pickerbot-slam` és a naplóban a „Dropped”/„Failed” sorok, valamint az `Average Scan Matching Score` legalacsonyabb értékei. Utána döntsük el, kell-e a tuned fájl.

### SLAM mozgás közben, 2026-09-21 14:27

Az újraindított `pickerbot-slam` (új, üres térkép) után a felhasználó joystickkel vezette a robotot, gyors fordulásokkal; a térkép enyhén szétcsúszott. A `robot-slam-diag.ps1` a mozgás után futott.

**Mérve (SLAM-napló és `docker stats`):**
- A konténer CPU-terhelése 6,5%, memóriája 43,6 MiB. A gmapping **nem processzorkorlátos**, és a naplóban a mozgás közben minden feldolgozott scan után volt frissítés (nincs kihagyott „update frame” sorozat a mozgás alatt).
- A gyors forduláskor az odometria egy lépésben (`ad`) 0,47–0,87, majd 2,13 és 1,22 rad elfordulást jelentett két egymás utáni scan között (kb. 83 ms). Ekkor az illesztési pontszám 1490-ről 1110, 864 és 919 értékre esett, a hasznos részecskék száma (`neff`) 1-re, és többszöri újramintavételezés történt („RESAMPLE”). Utána `neff` 7-re állt vissza, a térkép azonban már a hibás pozícióval épült tovább.
- A `/scan` 12,0 Hz, `/odom` 20,0 Hz, `/imu` 20,0 Hz (az IMU időzítése ekkor szabálytalan volt: 0,025–0,075 s között ingadozott, a korábbi mérésnél egyenletes volt), `/tf` kb. 100 Hz.
- Wi-Fi: −28 dBm, 243–300 Mbit/s, **Power save be**, csatorna 2452 MHz (2,4 GHz, 40 MHz széles). A `TX` bájtszámláló a 14:14 és 14:27 közötti kb. 13 percben 1,47 GB-tal nőtt, ez kb. 15 Mbit/s tartós feltöltés a robot felől (a 14:10-es és 14:14-es érték között a számláló valószínűleg 32 bites túlcsordulással nullázódott; ezt nem ellenőriztük).

**Következtetés (nem bizonyított, de a mérésekkel egyezik):** a szétcsúszást nem a CPU, nem a scan/odom kiesése és nem a Wi-Fi okozta, hanem a gyors fordulás: néhány scan alatt több radiánnyi odometria-ugrás, és a scan is torzul, mert a LiDAR 83 ms alatt körbeér, miközben a robot közben elfordul. A részecskék összeomlanak (`neff` 1), a 8 részecske és a `gmapping-tuned.launch` (20 részecske) ezt valószínűleg **nem** oldja meg; a fordulási sebesség korlátozása többet segít. A robot valós maximális szögsebessége és a joystick sebességfokozata nincs feltárva.

**Kamerakésés:** a felhasználó szerint a kép a mérés idején késni kezdett. A késés nincs számszerűen mérve. A ~15 Mbit/s tartós feltöltés a 640×480-as MJPEG-folyamból adódhat; a kisebb felbontású/kevesebb képkockás forrás a forgalmat csökkentené, de ezt még nem próbáltuk ki.

**Következő lépések:** (1) a joystick/távirányító sebességfokozatának vagy szögsebesség-korlátjának felkutatása, és térképezéskor lassú fordulás (kb. 0,1 rad/scan alatt, azaz 1 rad/s alatt); (2) az IMU szabálytalan időzítésének vizsgálata; (3) kamera: kisebb felbontás/képkockaszám a forrásnál (robotoldali változtatás, külön jóváhagyással).

### 2026-09-21: forgási sebesség csúszka a bemutatóoldalon

- `scripts/control_panel.html`: a bázispanelen új **forgási sebesség** csúszka (0,10–1,00 rad/s, alapérték és újratöltés utáni érték 0,30 rad/s, tehát a korábbi plafon változatlan). Piros vonal jelöli a 0,60 rad/s határt; fölötte figyelmeztetés jelenik meg és a naplóba is bekerül. Csökkentéskor a már kiküldött forgás azonnal az új határra vágódik.
- **Csak a webes forgatógombokra vonatkozik**, a fizikai távirányítót nem korlátozza (az az alsó vezérlőhöz kapcsolódik). A lineáris plafon (0,15 m/s) és a `base_drive.py` értékei nem változtak.
- A 0,60 rad/s határ **becslés, nem mért érték** (12 Hz-es LiDAR mellett kb. 0,05 rad/szkennelés). A mért összeomlás sokkal magasabb sebességnél történt (kb. 0,5 rad/szkennelés).
- Ellenőrzés: az inline JavaScript `node --check` alatt hibamentes; headless böngészőben (ROS-tár nélkül, csonkolt `ROSLIB`-bal) a csúszka, a figyelmeztetés és a kiírt plafon a várt módon változott, JS-hiba nélkül. Valódi robottal, élesítéssel **nem** próbáltuk ki.
- Új `.gitignore`: `slam-diagnosztika.txt` (a diagnosztikai szkript kimenete), Python-gyorsítótárak.

### 2026-09-21: csak olvasó telemetria a bemutatóoldalon

- `scripts/control_panel.html`: a „Parancsnapló” panel „Telemetria és parancsnapló” lett, három csempével: **akkufeszültség** (`/PowerVoltage`), **tényleges sebesség** (`/odom`, kerékodometria), **forgás** (`/odom` és `/imu` együtt, 5 másodperces maximum). Az `/odom` és `/imu` 10 Hz-re ritkítva jön (rosbridge `throttle_rate`), a `/PowerVoltage` 0,5 s-ra. A szekció **semmit nem publikál**. 3 másodperc adat nélkül a csempe „nincs friss adat” állapotba megy.
- A forgás-csempe a **fizikai távirányítóval végzett** mozgásnál is mutatja a tényleges szögsebességet, és piros figyelmeztetést ad, ha az meghaladja a 0,6 rad/s határt (becslés, lásd fent); ezt a naplóba is beírja, legfeljebb 5 másodpercenként. A távirányítót nem korlátozza, csak jelez.
- Az akku-határok (figyelmeztetés 21,5 V alatt, piros 20,0 V alatt) **becslések**: 2026-09-18-án 21,9 V-on még stabil volt a rendszer, 17,74 V-nál a szervók elengedtek. Nem mért határok.
- A `/PowerVoltage` üzenettípusát nem adtuk meg (a rosbridge felismeri), és a kód csak a `data` számmezőt fogadja el; a tényleges üzenetformát a roboton **még nem ellenőriztük**.
- Ellenőrzés: `node --check` hibamentes; headless böngészőben csonkolt `ROSLIB`-bal, kitalált üzenetekkel a csempék (rendben / gyors forgás / alacsony akku / elavult adat) a várt állapotot mutatták, JS-hiba nélkül. Valódi robottal és valódi üzenetekkel **nem** próbáltuk ki.

### 2026-09-21: „Térkép újrakezdése” gomb javítása

- **Tünet:** a gomb nem indította újra a SLAM-konténert; az oldal csak általános hibát írt. A pontos ok nincs igazolva (gyanú: a `plink.exe` nincs a PATH-on, vagy a README-ben lévő sudo-jelszó nem érvényes).
- **`scripts/demo_server.py`:** ha a felhasználó SSH-kulcsa olvasható (`Documents\Codex\pickerbot-access`), a szerver `ssh.exe`-t használ (kulccsal, ellenőrzött hostkulccsal), és a sudo-jelszó a README-ből csak a stdin-en megy át; egyébként a korábbi `plink.exe`-s út marad. A hiba okát a szerver most megnevezi: sudo-jelszó nem egyezik, hostkulcs nem egyezik, SSH-belépés elutasítva, robot nem érhető el, konténer nem létezik, időtúllépés, hiányzó ssh/plink. Nyers hibakimenetet és jelszót nem ad vissza.
- **`scripts/dashboard.html`:** a térképpanel a szerver hibaszövegét jeleníti meg.
- **Ellenőrzés:** a `demo_server.py` Python-szintaxisa hibamentes, a hibaosztályozás és a sikeres ág mockolt `subprocess`-szel kipróbálva (a jelszó nem kerül a parancssorba); a `dashboard.html` JavaScriptje `node --check` alatt hibamentes. **Valódi robottal, a Windows-os Python szerverrel és az `ssh.exe`-vel nem próbáltuk ki.** A mockolt próba nincs elmentve a repóba.
- **Telepítés:** a `start-demo-view.ps1` a már futó helyi szervert megtartja, ezért az új `demo_server.py` csak `stop-demo-view.ps1`, majd újraindítás után él.

**Gyorsítótár-hiba (2026-09-21):** a frissített `control_panel.html` után a telemetria-csempék „eltűntek”: a böngésző (a beépített megfigyelő panel is) a régi oldalt szolgálta ki a gyorsítótárból, F5 után visszatértek. A fájl a lemezen végig rendben volt. A `scripts/demo_server.py` most minden válaszhoz `Cache-Control: no-cache` fejlécet ad (a JSON-válaszok saját `no-store` fejléce megmarad), így a szerver újraindítása után a módosítások F5 nélkül is érvényesülnek. Helyi próbán ellenőrizve (a fejléc egyszer szerepel); a Windows-os szerverrel nem próbáltuk.

### 2026-09-21: több nyitott ablak okozta a kamerakésést, megosztott kamerarelé

- **Ok (a felhasználó próbája szerint):** egy ablakkal a kép jó, három ablakkal lelassul. A robot `web_video_server` minden kliensnek külön kódolja és küldi a 640×480-as MJPEG-folyamot. Mérve: egy folyam 22,6 kép/s és kb. 4,3 Mbit/s volt; közben az egyes JPEG-pillanatképek 1,7–5 s alatt jöttek meg (korábban 0,07–0,7 s). A késés tehát a fülenkénti külön folyamból adódik, nem magából a Wi-Fi-jelből. A robot processzorterhelését ebben a mérésben nem néztük.
- **Javítás (csak a laptopon):** a `scripts/demo_server.py` új `/cam/c70.mjpg` végpontja **egyetlen** robot felé irányuló MJPEG-kapcsolatot olvas, és minden nyitott fülnek a legfrissebb képkockát adja. Kliens nélkül 15 másodperc után lezárja a robot felé a kapcsolatot; ha az alagút nem elérhető, 502-t ad (a `dashboard.html` meglévő tartalék-logikája ezt hibaként kezeli). A `scripts/dashboard.html` C70-folyama ezt az URL-t használja. A robotot nem módosítottuk.
- **Ellenőrzés (szimulált robot-oldali folyammal, Linuxon, nem a valódi robottal):** 3 párhuzamos kliens 1 db robot felé irányuló kapcsolatot használt; a gyors kliensek képkockái átlagosan 1 ms régiek voltak; alagút nélkül 502; kliens nélkül a kapcsolat lezárult. **Ismert korlát:** egy nagyon lassan olvasó kliens (pl. megszakadt vagy háttérbe tett fül) mögött az operációs rendszer pufferei régi képkockákat halmozhatnak; ez a szimulációban látszott (a kis küldőpuffer sem szüntette meg). Ez a többi fület nem érinti. A Windows-os szerverrel és a valódi kamerával **nem** próbáltuk ki.
- **Nyitott:** a folyam továbbra is 640×480 (a szerver figyelmen kívül hagyja a méretkérést); a forrás felbontásának csökkentése külön, robotoldali feladat. A pillanatkép-alapú ellenőrzések (`start-demo-view.ps1`, `watch-demo-view.ps1`, a műszaki nézet ellenőrzése) továbbra is közvetlenül a 8080-as porton mennek.
- **Telepítés:** `stop-demo-view.ps1`, majd `start-demo-view.ps1` (a futó szervert újra kell indítani).

### 2026-09-21: akkumulátor-töltöttség becslése

- **Az akku címkéje szerint** (a felhasználó olvasta le, részben kínai felirat): P760S, 6000 mAh, 134,4 Wh, 22,4 V névleges, 25,55 V (töltési véghatár), 3 A (töltés), 15 A (max. kisütés); nagybetűvel 6000 mAh és 24 V. A cellaszám (6) és a kémia a feszültségekből következtetett feltételezés, nincs a címkén; a kémia (Li-ion/LiPo) nincs megerősítve.
- `scripts/control_panel.html`: az akku-csempe a `/PowerVoltage` alapján **becsült százalékot** is mutat (pl. „≈ 62% · 23,30 V”), a legutóbbi ~10 másodperc mediánjából, tipikus Li-ion/LiPo kisülési görbével, a felső pont a 25,5 V-os töltési határhoz igazítva. Figyelmeztetés 22,2 V alatt (~15%), piros 21,6 V alatt (~5%); ez felváltja a korábbi 21,5/20,0 V-os becslést. A százalék **nem mért töltöttség**: terhelés alatt a feszültség lesüllyed, a középső tartományban (kb. 20–60%) a görbe bizonytalan; a robot nem ad töltöttségi (fuel gauge) adatot.
- Ellenőrzés: `node --check` hibamentes; headless böngészőben kitalált feszültségekkel (25,5 V ≈ 100%, 23,3 V ≈ 62%, 22,0 V ≈ 9% figyelmeztetés, 21,0 V ≈ 3% piros, 17,7 V ≈ 0%). Valódi akkuval, egy teljes lemerítési görbe felvételével **nem** vetettük össze.
- **Javasolt pontosítás:** egy töltési/lemerítési ciklus közbeni feszültségnaplózás (idő és feszültség), amiből a százalék-görbe az adott akkura hitelesíthető.

### 2026-09-21 (este): felhasználói visszaigazolások

- **Indítás kábel nélkül:** a felhasználó szerint a robot azóta újraindult, hogy csak Wi-Fin (`wlan0`) és akkumulátorról fut, és **működik**. Ez a korábban nyitott „kábel nélküli újraindítás” pontot lezárja (a felhasználó megfigyelése, számszerű mérés nélkül).
- **Térkép újrakezdése gomb:** a felhasználó szerint működik.
- **Nem igazolt:** a megosztott kamerarelé (`/cam/c70.mjpg`) többablakos működése valódi robottal; ezt a felhasználó még nem próbálta külön.
- **Helyszíni Wi-Fi:** a bemutató helyszínén csak a rendezvény napján lehet kipróbálni; a tartalék-terv (kábeles kapcsolat) és a robot gyors újraindíthatósága ezért fontos.

### 2026-09-22: bázisvezérlő gombok — irány, billentyűzet, oldalazás, majd egy ÜTKÖZÉS

**Előre/hátra felcserélve, javítva:** a felhasználó jelezte, hogy az „előre” gomb hátra vitte a robotot. Mérés nélküli, közvetlen visszajelzés alapján az `x` előjelét megfordítottuk (`X_SIGN = -1` a `control_panel.html`-ben). Az y (oldalazás) és a forgás előjelét ekkor még nem ellenőriztük.

**Vezérlés-kiosztás leegyszerűsítve:** a felhasználó szerint a fizikai kontrolleren nincs átlós mozgás, ezért a webes felületről is kivettük az átlós gombokat (korábbi Q/E/Z/C-diagonál). A billentyűzet-kiosztás: W/S előre-hátra, Q/E forgás a tengelye körül (helyben, halad­ás nélkül — a felhasználó ezt külön megerősítette), A/D pedig **tiszta oldalazás** (mecanum strafe, fordulás nélkül) — ezt a felhasználó kifejezetten kérte vissza, mivel a mecanum kerekek elvileg tudják. A dpad felirata és elrendezése ennek megfelelően átalakult (↑ előre / ← oldalra / ■ / oldalra → / ↓ hátra, forgás gombok külön sorban). A billentyűzeten lenyomott gomb most a megfelelő felületi gombot is vizuálisan kiemeli (korábban ez hiányzott).

**⚠️ ÜTKÖZÉS — biztonsági esemény, felhasználó által jelentve, számszerű mérés nélkül:**
- Az oldalazás (A/D) kipróbálásakor a felhasználó szerint a robot **nem tisztán oldalra**, hanem nagy ívben, „donut”-szerűen mozgott. Ez **nincs megoldva**: ez feltehetően a robot alvázának/firmware-ének mecanum-kalibrálási vagy inverz-kinematikai problémája, nem a webes oldalé (a `/cmd_vel`-be `y`-only, `x=0`, `az=0` megy ki A/D-nél). Felügyelt, fizikai leállítóval végzett, lassú újramérés kell hozzá — én nem küldök mozgásparancsot, ezt csak a felhasználó tesztelheti.
- Ezután a felhasználó W-t nyomott (előre), majd **elengedte** — a robot state szerint ekkor meg kellett volna állnia, de **semmire nem reagált**, tovább ment, amíg **neki nem ütközött egy asztalnak**. Ott a robot saját (feltehetően ütközés/túláram-védelmi) vészleállása lépett be: megmerevedett és sípolt, a felhasználó kézzel állította le.
- **Valószínű ok (nincs közvetlenül igazolva, de a kód alapján ez a legvalószínűbb):** a böngésző nem küldött `keyup` eseményt a W billentyűre (ritka, de ismert böngésző/OS-jelenség, pl. fókuszváltás vagy gyors billentyűváltás közben). A felület korábban semmilyen módon nem védekezett ez ellen: ha egy billentyű "lenyomva" állapotban ragadt a JS állapotában, a robot a végtelenségig ment volna, amíg neki nem ütközik valaminek — pontosan ez történt.
- **Javítás (`control_panel.html`, még NEM próbáltuk ki éles roboton):** billentyűzet-védőháló. Amíg egy billentyűt ténylegesen nyomva tartasz, az OS ismétlődő `keydown` eseményeket küld rá; ha egy adott billentyűre 700 ms-ig nem jön újabb `keydown` (se első, se ismétlődő), a felület automatikusan elengedettnek veszi, `keyup` nélkül is (`releaseStaleKeys()`, minden 100 ms-os tick-ben fut). Ez nem oldja meg, *miért* veszett el a `keyup`, csak korlátozza a kárt: legrosszabb esetben is kb. 0,7–1,3 másodperc után (a meglévő lassítási rámpával együtt) megáll a robot ahelyett, hogy a végtelenségig menne.
- **Nyitott, komolyabb megoldás (nincs elkezdve):** a fenti csak kliensoldali védelem, a hálózat/böngésző esetleges egyéb hibáira nem terjed ki. A robusztusabb megoldás egy robotoldali `/cmd_vel`-időtúllépés (watchdog) lenne: ha a robot X ideig nem kap friss sebességparancsot, magától álljon meg — ezt a robot alacsonyszintű vezérlője vagy egy külön ROS-node tudná csinálni, ez robotoldali változtatás, felügyelt kipróbálást igényel, egyelőre csak javaslat.
- **Következő lépés, csak felügyelettel, fizikai leállítóval:** (1) az oldalazás donut-jelenségének újramérése nagyon lassan, rövid nyomásokkal; (2) a mai javítás (billentyű-védőháló) valós tesztje — direkt hosszan nyomva tartott W, majd elengedés, ellenőrizve hogy tényleg megáll-e ésszerű időn belül; (3) eldönteni, kell-e a robotoldali `/cmd_vel`-watchdog.

**Fókuszvesztéskor túl gyakori automatikus leélesítés — pontosítva:** a felhasználó szerint az élesítés "random" kikapcsolt. A felhasználó megerősítése alapján ablakváltáskor/fókuszvesztéskor történt, akkor is, ha épp nem vezetett — ez a korábbi, feltétel nélküli `blur`/`visibilitychange` → leélesítés miatt volt. Módosítva: fókuszvesztéskor csak akkor jár teljes leélesítéssel (újra-megerősítést igénylő biztonsági leállás), ha éppen ténylegesen nyomva volt egy vezérlőgomb/billentyű (ilyenkor a `keyup` elveszhet, ld. a fenti ütközéses eset) — ha semmi nem volt nyomva, csak nullázzuk a parancsot, de élesítve marad. **Még NEM próbáltuk ki éles roboton.**

**Inaktivitás-alapú automatikus leélesítés kivéve (felhasználói döntés, biztonsági kompromisszum tudatosan vállalva):** a felhasználó jelezte, hogy élesítés után egy ideig tétlenkedve (nem fókuszváltás, csak nem nyomott semmit) is leélesített a rendszer. Rákérdeztünk, hogy a korábbi 20 mp-es időzítőt hosszabbítsuk-e vagy vegyük ki teljesen — a felhasználó a teljes kivételt választotta, annak tudatában, hogy ez a mai ütközés utáni egyik biztonsági réteg elvétele. A `control_panel.html`-ben az `ARM_TIMEOUT_MS`-alapú auto-leélesítő blokk ki van kommentezve/eltávolítva a tick-ből; a konstans csak dokumentációként maradt a kódban. **Ami MARAD élesítés-védelemként:** kézi leélesítés gomb, webes megállítás (leélesítés), rosbridge-kapcsolat megszakadásakor automatikus leélesítés, fókuszvesztés vezérlés közben (ld. fent), és a fizikai vészleállító (ez utóbbi a legmegbízhatóbb, hálózatfüggetlen). **Még NEM próbáltuk ki éles roboton.**

**Kamera és térkép "szétkapcsolása" háttérbe kerülő fülnél — javítás, még nem tesztelve:** a felhasználó szerint ha egy ideig másik ablakot néz, a kamera és a térkép is szétkapcsol, és visszatéréskor sokára, nagy késésekkel jön vissza a kép. Két valószínű ok: (1) a korábban is dokumentált, szimulációval igazolt relé-korlát — egy háttérbe kerülő (lassan "olvasó") fül mögött az OS pufferei régi képkockákat halmoznak fel a kamera MJPEG-folyamában, ez most éles roboton is megerősítést nyert; (2) a `dashboard.html`-ben a rosbridge websocket-kapcsolat korábban **egyáltalán nem** próbált újracsatlakozni, ha megszakadt — csak egy közvetett, 8-15 másodperces lépésekből álló, a kamera-figyelőn átfutó oldalújratöltés-lánc hozta vissza, ami háttérben lévő (időzítőkben lelassított) fülnél még lassabban indult be.
- **Javítás:** a `ros.on('close'/'error', ...)` most 3 másodperces késleltetéssel megpróbálja újracsatlakoztatni magát (`ros.connect(...)`), és minden (újra)csatlakozáskor újra feliratkozik a `/map` témára. A `document.visibilitychange` eseményre: háttérbe kerüléskor a kamera `<img>` `src`-jét lezárjuk (nem pufferelődik tovább), előtérbe kerüléskor azonnal friss (cache nélküli) `src`-t adunk neki, és ha a rosbridge nincs csatlakozva, azonnal újrapróbáljuk — nem várunk a lassú, időzített ellenőrzésekre.
- **Ellenőrzés:** csak `node --check` szintaxis-ellenőrzés és kézi átolvasás. **Valódi böngészőben, ténylegesen háttérbe küldött füllel, éles roboton NEM próbáltuk ki.** Következő lépés: nyisd meg a bemutatóoldalt, élesítés NÉLKÜL (ez tisztán kamera/térkép-nézeti teszt, mozgásparancs nem kell hozzá), válts át egy másik ablakra kb. 1-2 percre, majd válts vissza, és nézd meg, milyen gyorsan és milyen késéssel tér vissza a kép és a térkép a korábbi (nagyon lassú, nagy lemaradásos) viselkedéshez képest.

**Oldalazás (A/D) — külön, óvatosabb sebességhatár a donut-jelenség miatt:** a felhasználó megerősítette, hogy a `control_panel.html` szögsebesség-csúszkája (`angLimit`) az A/D gombokra **nincs hatással** — ez elvárt, mert az A/D-nél a kiküldött `/cmd_vel.angular.z` mindig pontosan 0 (a kódban ellenőrizve), tehát a látott "donut"-forgás nem a webes oldalról kiküldött, korlátozható parancs, hanem tényleges, a robot alvázán/firmware-én keletkező, nem szándékolt forgás — ez robot-oldali (valószínűleg mecanum-kalibrálási) hiba, amit a weboldalról nem lehet kijavítani. A felhasználó kérésére kapott egy **külön, az előre/hátrától független** sebességcsúszkát ("oldalazás sebessége", `strafeLimit`, 0.03–0.15 m/s, alapértelmezés 0.08 m/s — a fele az előre/hátra 0.15 m/s-jának), abban a reményben, hogy alacsonyabb oldalazási sebességnél a forgás kevésbé kilengő (ez feltételezés, nincs mérve). A `recomputeTarget()` mostantól `strafeLimit`-tel szorozza az `y` célértéket, nem a közös `MAX_LINEAR_MPS`-szel. **Még NEM próbáltuk ki éles roboton**, és a donut-jelenség gyökéroka továbbra sincs kivizsgálva — ehhez felügyelt, lassú, fokozatosan növelt sebességű próbák kellenének, fizikai leállítóval.

**Mérés (felhasználó, 2026-09-22):** `angLimit` (szögsebesség-csúszka) 0,30 rad/s-ra állítva, A/D oldalazás közben a robot mégis eléri a **~0,88 rad/s** tényleges forgást. Ez megerősíti, hogy a csúszkának ehhez semmi köze (elvárt, ld. fent), és hogy a nem szándékolt forgás mértéke jóval a térképezésnél figyelmeztetésként használt `ANGULAR_MAP_WARN_RADPS` (0,6 rad/s) fölött van — vagyis oldalazás közben is szétcsúszhat a térkép, nem csak gyors fordulásnál. Megkérdeztük a felhasználót, tiltsuk-e le egyelőre az A/D-t; **a válasz: maradjon bekapcsolva, de vizsgáljuk ki a gyökérokot.** Kód-oldali változtatás emiatt egyelőre nem történt.

**Javasolt vizsgálati terv a donut-jelenség gyökérokához (felügyelt, a következő fejlesztő/agent vagy a felhasználó végzi élő roboton — én távolról nem tudom elvégezni):**
1. **Vizuális ellenőrzés, áramtalanítva:** a robotot úgy megemelve/megtámasztva, hogy egyik kerék se érjen talajt, nézd meg mind a 4 mecanum keréken a görgők (roller) irányát felülnézetből. Helyesen szerelt alvázon a kerekek "X" alakban állnak (átellenes kerekek egymás tükörképei). Egy fordítva szerelt vagy rosszul bekötött kerék pont ilyen nem kívánt forgást okoz tiszta oldalazásnál.
2. **Emelt, terheletlen próba:** ugyanígy megemelve, bekapcsolva, a legalacsonyabb sebességgel (0,03 m/s, az új `strafeLimit` csúszka minimuma) adj ki A/D parancsot, és nézd meg, mind a 4 kerék a várt irányba/sebességgel pörög-e. Földet érés nélkül biztonságos, és közvetlenül megmutatja, ha egy konkrét kerék/motor hibás.
3. **Csak ha az 1-2. nem mutat egyértelmű hibát:** földön, lassan, fizikai leállítóval, a `strafeLimit` csúszkával fokozatosan növelt sebességgel (0,03 → 0,08 → 0,15 m/s) mérni, hogy a nem szándékolt forgás mértéke arányos-e a sebességgel (inkább szoftveres kevert-mátrix/IK-hiba a robot vezérlőjében) vagy nagyjából állandó (inkább egy konkrét rossz kerék/motor/enkóder).

**GYÖKÉROK MEGTALÁLVA (felhasználó, fotó alapján, 2026-09-23):** a felhasználó elvégezte az 1. lépést (vizuális ellenőrzés) és fotót is készített. Megállapítás: a kerekek **nem** a helyes "X" mintában vannak — a két elülső kerék egyforma irányba (görgők egyformán "jobbra" dőlnek), a két hátsó kerék egyforma, de ellentétes irányba ("balra") van szerelve. Helyesen szerelt mecanum-alvázon az **átlósan szemközti** kerekek (bal-elő+jobb-hátsó, illetve jobb-elő+bal-hátsó) egyeznek típusban, nem az elő/hátsó párok — ez pont a fordítottja annak, amit a fotó mutat. Ez megmagyarázza a donut-jelenséget: tiszta oldalazás-parancsnál (`y`-only, `az=0`) a hibásan párosított görgők miatt a robot ténylegesen forog is.
- **Javasolt javítás (fizikai, még NEM elvégezve):** mivel pontosan 2-2 kerék van mindkét görgő-típusból, elég **csak az egyik oldal** (bal VAGY jobb, bármelyik) két kerekét elöl-hátul felcserélni (bal-elő ↔ bal-hátsó, VAGY jobb-elő ↔ jobb-hátsó) — ez matematikailag helyreállítja a helyes X-mintát mind a négy kerékre, nem kell mind a négyhez nyúlni és nem kell bal-jobb cserét csinálni. (Emlékeztető: a bal-elő/jobb-elő csere, illetve a bal-hátsó/jobb-hátsó csere ÖNMAGÁBAN nem segítene, mert azok a párok jelenleg egyforma típusúak — a csere semmit nem változtatna.)
- **Elvégzés módja (áramtalanítva, csak a felhasználó/következő fejlesztő tudja megcsinálni):** mielőtt bármelyik kereket leszereled, fotózd/jelöld meg, melyik eredetileg hova tartozott, hogy vissza lehessen állítani, ha valami nem stimmelne. A kerék rögzítési módja (pl. hatszög-tengelyes gyorscsatlakozó vagy csavaros rögzítés) a fotóból nem egyértelmű — ez Wheeltec/Roboworks alkatrész, a talpkereten lévő "scan me for support" QR-kód mögött van gyártói támogatás, ha kell.
- **Ellenőrzés a csere után:** a 2. lépés (emelt, terheletlen próba) megismétlése — nézd meg, mind a 4 kerék helyesen forog-e —, csak utána földön, lassan, fizikai leállítóval, a `strafeLimit` csúszkával kezdve a legalacsonyabb sebességen.
- **Fontos:** ez a felhasználó saját, roboton végzett vizuális megállapítása; én csak a feltöltött fotó alapján erősítettem meg, hogy a leírás megfelel egy ismert mecanum-szerelési hibának. A tényleges csere és az utána következő mérés még nincs elvégezve.

**Fizikai csere megtörtént (felhasználó, 2026-09-23):** a felhasználó felcserélte a bal-hátsó és a bal-elő kereket. Megerősítése szerint a kerekek most helyes X-mintában állnak. **Még NEM ellenőrizve élő roboton** (sem emelt/terheletlen próbával, sem földön) — a következő lépés a vizsgálati terv 2. pontja: megemelve/megtámasztva (egyik kerék se érjen talajt), bekapcsolva, a legalacsonyabb sebességgel (0,03 m/s, `strafeLimit` csúszka minimuma) A/D próba, ellenőrizve hogy mind a 4 kerék a várt irányba pörög-e. Csak ez után érdemes földön, lassan, fizikai leállítóval kipróbálni.

**✅ MEGOLDVA (felhasználó, 2026-09-23):** a kerékcsere után a felhasználó földön (terhelt, valós) próbát is végzett: A/D-vel a robot most **tisztán oldalra megy, forgás nélkül**, ahogy kell. A donut-jelenség gyökéroka a rosszul szerelt (nem X-mintás) kerékpozíció volt; a bal-elő/bal-hátsó kerékcsere megoldotta. A webes oldal kódjában (`strafeLimit`, `X_SIGN` stb.) emiatt nincs teendő. Nyitott: a `strafeLimit` csúszka még mindig 0,08 m/s-on van alapértelmezésként, óvatosságból bevezetve, amíg a hiba nem volt kivizsgálva — most, hogy a gyökérok el van hárítva, a felhasználóval egyeztetve visszaállítható/megnövelhető az előre-hátra sebességéhez hasonló szintre, ha szeretné.

**Kontroller-hiba, robot-újraindítással megoldva (felhasználó, 2026-09-23) — ok NEM bizonyított:** a kerékcsere körüli időszakban a felhasználó azt tapasztalta, hogy a fizikai kontrollerrel csak a hátramenet és a megfogó nyitás/zárás működött, előre/forgás nem reagált. A webes oldalnak ehhez semmi köze (a fizikai kontroller közvetlenül a robot alsó vezérlőjéhez megy, nem a ROS-on/rosbridge-en át). A robot újraindítása után a kontroller ismét rendesen működött. **A gyökérok nincs kiderítve** — lehet összefüggésben az áramtalanítással/kerékcserével, de ez feltételezés, nincs bizonyítva. Ha megismétlődik, érdemes rögzíteni: pontosan mikor (kerékmozgatás közben/után, bekapcsolás után rögtön stb.), és van-e bármi jelzés (sípolás, LED) a roboton akkor.
**Gyors fordulásnál a térképezés leállítása (felhasználói kérés, nincs elkezdve):** a felhasználó szerint a mostani sárga figyelmeztetés (0,6 rad/s fölött) mellett a SLAM-algoritmust is le kellene állítani, amíg a gyors fordulás tart, ne csak figyelmeztessen. Ez robotoldali (gmapping/Docker) változtatást igényelne — pl. egy `/scan`-t szűrő köztes node, ami a gyors fordulás alatt nem engedi tovább a lézerképeket a gmapping felé. Ez nagyobb, kockázatosabb módosítás (élő SLAM-konténert érint), tervezést és felügyelt tesztelést igényel — egyelőre csak ötlet, nincs implementálva.

**Csúszkák felső határa a mért kontroller-csúcshoz igazítva (felhasználói mérés + kérés, 2026-09-23):** miután a donut-hiba megoldódott (kerékcsere, ld. fent), a felhasználó a meglévő telemetria-panellel felügyelt mérést végzett a **fizikai kontrollerrel** (ami közvetlenül az alsó vezérlőhöz megy, nem ROS-on át), és a következő valós csúcsértékeket mérte: **forgás 0,98 rad/s, oldalazás 0,57 m/s, előre-hátra 0,55 m/s.** Kérése: "csináld meg elsőnek ezt és a 3.-at" — azaz (2) a webes csúszkák felső határát igazítsuk ehhez a méréshez, és (3) az oldalazás sebességhatárát (amit a donut-gyanú miatt korábban óvatosan 0,03–0,15 m/s-ra korlátoztunk) most, hogy a gyökérok elhárult, gondoljuk újra.
- **Új előre-hátra sebesség-csúszka hozzáadva** (`linLimit`, korábban ehhez nem volt külön csúszka, csak a fix `MAX_LINEAR_MPS` konstans): tartomány 0,05–0,55 m/s, induló érték 0,15 m/s (óvatos, nem a mért maximum — újratöltéskor mindig visszaáll). A régi `MAX_LINEAR_MPS` konstans megszűnt, helyette a csúszkával állítható `linearLimit` változó szorozza az `x` célértéket.
- **Oldalazás csúszka (`strafeLimit`) felső határa** 0,15 m/s-ról 0,57 m/s-ra emelve (a mért kontroller-csúcsra), induló érték 0,15 m/s maradt (nem a maximum) — a UI szövege frissítve, jelezve, hogy a donut-hiba megoldva, élő roboton is ellenőrizve.
- **Forgás csúszka (`angLimit`) felső határa** 0,3 rad/s-ról 0,98 rad/s-ra emelve (a mért kontroller-csúcsra), induló érték változatlanul 0,3 rad/s. A 0,6 rad/s-os térképezési figyelmeztetés-küszöb (`ANGULAR_MAP_WARN_RADPS`) nem változott.
- **Fontos, amit a felhasználó megerősített kérdésre válaszul:** a csúszka jobb szélső értéke **kemény felső korlát** a webes vezérlésre — a webes oldalról soha nem lehet ennél gyorsabb parancsot kiküldeni, tehát a webes vezérlés sebessége innentől soha nem haladhatja meg azt, amit a fizikai kontrollerrel is meg lehet mutatni.
- **Ellenőrzés:** csak `node --check` szintaxis-ellenőrzés és a HTML-struktúra kézi átolvasása (a három csúszka-doboz div-beágyazását egy elgépelés miatt javítani kellett, ld. commit előtti diff). **Élő roboton, ténylegesen a mért felső határig kihúzott csúszkával MÉG NEM próbáltuk ki** — a következő lépés egy felügyelt, fokozatosan növelt sebességű teszt lenne, fizikai leállítóval, mindhárom tengelyre külön-külön.
- **Fontos, szándékosan NEM módosítva:** a `scripts/xavier_control/base_drive.py` fájl saját `MAX_LINEAR_MPS`/`MAX_ANGULAR_RADPS` konstansai (0,15 m/s / 0,3 rad/s) **változatlanok maradtak**. Ez a modul a `docs/08-kezi-vezerles.md` szerint jelenleg **nincs bekötve** semmilyen publish-hívásba (csak dict-eket épít, a saját unit tesztjein kívül semmi nem hívja élesben) — tehát ma a `control_panel.html` sebességhatárai a ténylegesen élő korlátok, a `base_drive.py` értékei nem befolyásolják a robot mozgását. Ha ez a modul valaha be lesz kötve egy tényleges `/cmd_vel`-publisherbe, akkor ott is frissíteni kell a konstansokat — ezt a `control_panel.html`-ben lévő megjegyzés is jelzi.

**Alapértékek felhúzva 0,5-re (felhasználói kérés, 2026-09-23):** a fenti kalibráció után a felhasználó kérte, hogy mindhárom csúszka **induló (alapértelmezett) értéke** legyen 0,5 — nem csak a felső határ. Módosítás: `linLimit` alapértéke 0,15 → 0,5 m/s, `strafeLimit` alapértéke 0,15 → 0,5 m/s, `angLimit` alapértéke 0,3 → 0,5 rad/s (a `MAX_ANGULAR_RADPS` konstans is 0,5-re módosult, mivel ez adja az `angularLimit` kezdőértékét). A felső határok (0,55 / 0,57 / 0,98) nem változtak. A HTML csúszka `value` attribútumai, a hozzájuk tartozó induló felirat-szövegek (pl. "0.50 m/s"), a statikus `baseReadout` kezdő szövege és a most már elavult "donut-hiba még ki nem vizsgálva" figyelmeztető szöveg (ez utóbbi frissítve lett, jelezve, hogy a hiba meg lett oldva) is frissítve lettek ennek megfelelően. `node --check` és CRLF-ellenőrzés lefutott, hiba nélkül. **Élő roboton még NEM próbáltuk ki az új alapértékeket.**

**Fizikai kontroller bal stick iránya fordított — nyitott, nem javítva (felhasználói jelzés, 2026-09-23):** a felhasználó szerint a fizikai távirányító bal stick-je fordítva reagál a várthoz képest. Mivel a fizikai kontroller közvetlenül a robot alsó vezérlőjéhez megy, nem ROS-on/rosbridge-en át, ehhez a `control_panel.html`/rosbridge oldalról nincs hozzáférés — ez nem javítható a webes kódban. A Roboworks support oldal (`roboworks.net/support`) és a letölthető Pickerbot Pro kézikönyv (`Pickerbot-Pro-User-Manual-v0240815.pdf`) átnézve: egyik sem dokumentálja a kézi távirányító kalibrálását/invertálását — a kézikönyv csak a szoftveres (ROS/joystick csomag, SSH, billentyűzet) vezérlést tárgyalja. Javasolt út: fizikailag megnézni, van-e csatornánkénti REV (fordító) kapcsoló magán az adón (gyakori hobbi-RC adóknál), vagy közvetlenül megkeresni a Roboworks supportot (`hi@roboworks.co`, +61 1300 991 274). **A felhasználó egyelőre elnapolta ezt a témát**, nincs tovább vizsgálva.

**Hátsó Astra RGB-D kamera beizzítva és a webes csempék közé beépítve (felhasználói kérés + felügyelt SSH-vizsgálat, 2026-09-23):** a felhasználó a "3. saját projekt" következő lépéseként a hátsó kamera/szenzorok élesítését kérte. Két, a felhasználó által beküldött fotó alapján azonosítottuk, hogy hátul egy már működő LSLIDAR (a `/scan`/`/map`-et adja, ehhez nem kellett nyúlni) és egy Orbbec Astra-stílusú RGB-D mélységkamera van felszerelve (RGB + IR sztereó pár + státusz-LED, 3 lencse). A `dashboard.html`-ben már réges-régen volt hozzá kész UI ("RGB kamera (Astra)", "Mélységkamera", "Infra (IR)"), de driver/topic nélkül, kikapcsolt állapotban.
- **Felügyelt SSH-vizsgálat (a felhasználó futtatta a parancsokat, én csak olvasó jellegű parancsokat javasoltam):** `rostopic list` + `docker ps` megerősítette, hogy jelenleg nincs élő Astra-topic és nincs hozzá docker konténer. `lsusb`/`find` megerősítette, hogy a kamera USB-eszköze csatlakoztatva van (`/dev/astra_s`, Orbbec Astra S, serial `17121010370`) és a driver már le van fordítva a roboton (`~/wheeltec_robot/devel/lib/astra_camera/astra_camera_node`). A robot saját, kész indítója (`~/wheeltec_robot/src/turn_on_wheeltec_robot/launch/wheeltec_camera.launch`, alapértelmezett `camera_mode:=Astra_S`) pontosan a `dashboard.html` által várt topic-neveket adja (`/camera/rgb/image_raw` remap-elve, `/camera/depth/image_raw`, `/camera/depth/points`, `/camera/ir/*`). Megerősítve, hogy a fő bringup (`turn_on_wheeltec_robot.launch`) **nem** hívja ezt sehol, tehát nincs ütközés-kockázat.
- **Élő teszt (előtérben, SSH-n, felügyelve):** `roslaunch turn_on_wheeltec_robot wheeltec_camera.launch` — a felhasználó indította, a naplóban látszik, hogy az eszköz csatlakozott és a szín stream elindult ("Infrared and Color streams are enabled. Infrared stream will be disabled." — ez a hardver saját korlátja, egyszerre csak szín VAGY IR megy, ez magyarázza a `dashboard.html`-ben már meglévő, IR-t szándékosan letiltó megjegyzést). `rostopic hz` megerősítette: `/camera/rgb/image_raw` ~29,6 Hz, `/camera/depth/image_raw` ~29,7-29,9 Hz, mindkettő stabil.
- **Webes integráció:** a `dashboard.html`-ben az "RGB kamera (Astra)" és "Mélységkamera" panel eddig csak a teljes (`?embed=1` nélküli) nézeten futott — a beágyazott nézetből (amit a `control_panel.html` iframe-ben használ) ki volt véve. Mostantól a beágyazott nézet is lekéri és megjeleníti ezt a két panelt (a nehéz 3D pontfelhő-nézet és a 2D LiDAR felülnézet továbbra is csak a teljes dashboardon fut, mert azok nehezebbek, és a `/map` a beágyazott nézetben amúgy is ugyanazt fedi le egyszerűbben). A panelek `extra-sensor` osztálya (ami embedben rejtve volt) megszűnt rajtuk, egy második `demo-pair` sorba kerültek a C70+térkép sor alá. A `control_panel.html` beágyazó `iframe`-jének magassága 480px→920px (asztali), 900px→1700px (mobil) — ez **durva becslés**, a pontos méretezés a következő, "kompaktálás" lépésben jön.
- **Élő ellenőrzés a felhasználó képernyőjén (2026-09-23):** mind a négy csempe (C70, térkép, hátsó RGB, hátsó mélység) élő, friss képet mutatott a bemutatóoldalon, miután a felhasználó újraindította a `start-demo-view.ps1`-t (ami a `web_video_server` MJPEG-alagutat is újranyitja — enélkül a C70 és a RGB is "videókapcsolat megszakadt" hibát mutatott, ez nem kódhiba volt, csak hiányzó alagút).
- **`pickerbot-camera.service` telepítve (2026-09-23):** a felhasználó élő ellenőrzése után döntött úgy, hogy a kamera driver tartósan fusson, ne csak előtérben. A `pickerbot-rosbridge.service` pontos mintájára (`After=`/`Requires=pickerbot-bringup.service`, ugyanaz a forrás-sorrend, `Restart=on-failure`, `RestartSec=5`) elkészült a `pickerbot-camera.service`, amit a felhasználó telepített (`sudo systemctl daemon-reload && enable && start`). A `systemctl status` megerősítette: fut (`astra_camera_node` + `image_transport/republish` alfolyamatokkal), és `enable`-ölve van, tehát robot-újraindításkor is magától elindul. A fájl bekerült a repóba is: `systemd/pickerbot-camera.service` (a többi `pickerbot-*.service.d` melletti dokumentáció-mintát követve, itt egy teljes unit-fájlként, mert ez nem egy meglévő service drop-inja, hanem önálló új szolgáltatás).
- **Fontos, még NYITVA maradt:**
  1. A tile-elrendezés/méretezés ("kompaktálás, gyomlálás, rendezés") a felhasználó explicit kérése szerint még hátravan, ez a terv szerinti következő lépés.
  2. Az IR panel (`/camera/ir/image_raw`) szándékosan **nem** lett bekötve/bekapcsolva — a driver dokumentáltan összeomlik tőle élesben (ld. a `dashboard.html`-ben már korábban is meglévő figyelmeztetés), és a mostani teszt is megerősítette, hogy szín+IR egyszerre nem megy ezen a hardveren.
  3. A `pickerbot-camera.service` most éles robot-újraindítással **még nincs végigpróbálva** (csak `systemctl start`-tal), tehát nem 100%-ig bizonyított, hogy reboot után is problémamentesen feljön — ha a felhasználó a közeljövőben újraindítja a robotot valamiért, érdemes utána megnézni.

## Csempe-elrendezés átrendezése, szöveg-gyomlálás, kamera-visszakapcsolódás javítása (2026-09-23)

- **Elrendezés átrendezve a felhasználó explicit prioritási sorrendje szerint.** A `control_panel.html` `.grid`-je a korábbi 2 oszlopos, `grid-column`/`grid-row` elhelyezésű elrendezésből egyetlen oszlopos, `flex-direction:column` elrendezésre váltott — ott a DOM-sorrend maga adja a megjelenési sorrendet, nincs külön grid-trükk. Az új sorrend (fentről lefelé): (1) kompakt ÉLESÍTÉS-sáv, önálló panelcím nélkül; (2) kamerák+térkép+telemetria panel (a 3 telemetria-csempe ide került át a korábbi napló-panelből); (3) sebesség-csúszkák panel; (4) kézi gombok (dpad, forgatógombok, vész-stop) panel; (5) kar+gripper (szimuláció) panel; (6) parancsnapló panel, immár csak a nyers logot tartalmazza. Minden meglévő elem-ID (pl. `armBtn`, `linLimit`, `teBattery`, `logBox` stb.) megmaradt, tehát a JS-vezérlés változatlanul működik.
- **Szöveg-gyomlálás:** a banner, a csúszka-magyarázatok (`limit-note`), a kar-panel leírása, az átlós-mozgás megjegyzés és a `baseReadout` felirat jelentősen rövidült — a felhasználó kifejezett kérése szerint ("nem kell egy regényt leírni"). A tartalmi információ (mért csúcsértékek, korlátok, figyelmeztetések) megmaradt, csak a redundáns/hosszú magyarázó mondatok estek ki.
- **Validálva commit előtt:** mindkét fájl (`control_panel.html`, `dashboard.html`) legnagyobb `<script>` blokkja `node --check`-kel hibamentes; mindkét fájl végig CRLF sorvéget használ (ellenőrizve); a `control_panel.html` `<div>` nyitó/záró tag-ek száma egyezik (55/55) a body-ban. Élő felhasználói visszaigazolás **még nincs** — ez a következő lépés.
- **C70-kamera visszakapcsolódási hiba (felhasználó screenshotokkal jelezte, hogy háttérbe/előtérbe kerülés után a C70 panel beragad "videókapcsolat megszakadt" állapotban, F5 után sem tér magától vissza).** Gyökérok azonosítva: a `dashboard.html`-ben volt egy 8 másodperces periodikus újrapróbálkozás (`setInterval`), ami a beágyazott (`control_panel.html` iframe-es) nézetben `!EMBEDDED` feltétellel **teljesen ki volt kapcsolva** — tehát ha a `visibilitychange`-kezelő első, azonnali újracsatlakozási kísérlete elakadt (pl. egy átmeneti `error` esemény miatt azonnal "megszakadt" állapotba állt), utána a beágyazott nézetben semmi nem próbálkozott újra, csak a lassabb (8-15 mp-es) pillanatkép-teszt + teljes oldal-újratöltés lánc. **Javítás:** ez a periodikus újrapróbálkozás mostantól minden nézetben fut (nem csak a teljes dashboardon), és az intervallum 8 mp-ről 5 mp-re csökkent, hogy jobban illeszkedjen a felhasználó által elvárt "legfeljebb 5 másodperc" határhoz. A `visibilitychange`-kezelő RGB-kamerát (Astra) kezelő kiegészítése (a korábbi munkamenetben írva, hogy a hátsó RGB kamera is friss kapcsolatot kapjon háttérből visszatéréskor) változatlanul megmaradt. **Ez a javítás is csak kódszinten kész, élő teszt (tényleges tab-háttérbe/előtérbe váltás a roboton) még nem történt** — a felhasználónak ezt is ki kell próbálnia.
- **Nyitva maradt, változatlanul:** a `docs/` mappa átnézése/rendezése (a felhasználó által kiválasztott harmadik "gyomlálás" altéma) még nem kezdődött el, ez a következő lépés a mostani réteg jóváhagyása után.

## C70/RGB tunnel-hiba diagnózis + nagy elrendezés-átdolgozás (2026-09-23, folytatás)

- **A C70/RGB "videókapcsolat megszakadt" hiba valódi oka (élőben, böngésző Network fülével igazolva): a `web_video_server` SSH-alagút (127.0.0.1:8080) nem futott** — minden kép-kérésre `NS_ERROR_CONNECTION_REFUSED` jött, a rosbridge (192.168.123.50:9090) viszont rendben csatlakozott. Ez **nem kódhiba volt**, hanem az, hogy a `start-demo-view.ps1` alagút leállt/nem futott — újraindítása után a kamerák azonnal éltek. A `visibilitychange`/periodikus-újrapróbálkozás javítás (előző napló-bejegyzés) helyesen viselkedett, csak infrastruktúra hiányzott mögüle.
- **Nagy elrendezés-átdolgozás a felhasználó lépésenkénti visszajelzései alapján:**
  1. A fejléc egysoros, kompakt lett; a régi figyelmeztető banner törölve (a lényeg egy kicsi "⚠ részletek" linkbe került a fejlécben, ami a `docs/08-kezi-vezerles.md`-re mutat).
  2. A kamerák/térkép/telemetria/csúszkák egyetlen "első képernyő" (`.above-fold`) blokkba kerültek: kamerák+térkép balra (nagyobb, `.cam-row`), telemetria+csúszkák egy keskeny oldalsávban jobbra (`.side-col`), sokkal kisebb betűmérettel/paddinggel.
  3. Az ÉLESÍTÉS gomb és minden más (kézi gombok, kar, napló) lejjebb került, a `.above-fold` blokk ALÁ, külön `.grid` konténerbe — ezek csak görgetéssel érhetők el.
  4. **Dinamikus, képernyőmérethez igazodó magasság:** a `.above-fold` pontosan `100vh` magas flex-oszlop (fejléc + a kamera-panel), a kamera-panel `flex:1`-gyel tölti ki a maradék helyet — nem fix pixelérték. Ehhez kellett egy teljes `height:100%` lánc (`html`/`body`/`.above-fold`/`.sensor-panel`/`.cam-row`).
  5. **Fontos hiba, amit menet közben találtunk és javítottunk:** a `dashboard.html` beágyazott nézetében a 4 kamera/térkép-csempe képei `height:100%`-ot használtak, de egyik szülőjüknek sem volt KONKRÉT magassága (csak `min-height`), ezért a böngésző a képek natív (kb. 4:3) méretére esett vissza — az első sor (C70+térkép) felfalta szinte az egész helyet, a második sor (RGB+mélység) gyakorlatilag eltűnt. Javítás: a beágyazott `.grid` is `100%` magas flex-oszlop lett, a két `.demo-pair` sor `flex:1`-gyel egyenlően osztja el a helyet, `grid-template-rows:1fr` a determinisztikus sormagasságért — így a `height:100%` a képeken már ténylegesen érvényesül.
  - Mindkét fájlt validáltuk (`node --check` a fő `<script>` blokkon, CRLF-ellenőrzés, `<div>` nyitó/záró egyezés), és élőben, saját böngészőmmel (screenshot + DOM-ellenőrzés) is megnéztem a végeredményt — a 4 kamera/térkép-csempe most egyszerre, egyenlő méretben látszik, és a kamera-blokk a képernyő aljáig ér, görgetés nélkül.
- **Mellékesen javítva:** a `baseReadout` JS-generált szövege (`updateReadout()`) még a régi, hosszú megfogalmazást használta ("a felső határ mért kontroller-csúcs, az induló érték óvatos" stb.), pedig a statikus HTML placeholder szövegét korábban már lerövidítettük — csak a JS-ben lévő template string maradt régi. Most a JS is a rövid formát írja ki.
- **Átmeneti rémület, tanulság jövőre:** menet közben egyszer egy teljesen RÉGI (a mostani munkamenet előtti) tartalmat kaptam vissza a szerver közvetlen `fetch()`-ével is — ez pánikot keltett (fájl-visszaállás gyanúja, GitHub Desktop discard, OneDrive stb.). Két független, közvetlen lemez-ellenőrzéssel (`device_stage_files` a commit után azonnal, majd 20 mp múlva újra) igazoltuk, hogy a fájl a lemezen stabil és helyes volt mindvégig — a régi tartalom egyszeri, megmagyarázatlan gikszer volt a saját böngésző-tesztem (Claude beépített böngészője) cache-elésében, NEM valós probléma a felhasználó gépén. Ha ez újra előfordul, érdemes egyből két független forrásból (közvetlen fájl-stage + a felhasználó saját böngészője) ellenőrizni, mielőtt pánikba esünk.
- **Élő felhasználói visszaigazolás még hátravan** az above-fold elrendezésre és a C70 tunnel-újraindításra egyaránt.

## Kézzel rajzolt vázlat alapján teljes újratervezés — dashboard.html iframe eltávolítva (2026-09-23, folytatás)

- **A felhasználó kézzel rajzolt (iPad) vázlatot küldött** a végleges elrendezésről, és kimondta: a `dashboard.html` iframe-es beágyazása ("`?embed=1`") **többé nem kell** — minden (kamerák, térkép, vezérlés) egyetlen oldalba, a `control_panel.html`-be épül. Kifejezett kérés: csak a meglévő, már működő funkciókat rendezzük át, új funkciót ne építsünk bele.
- **Iframe eltávolítva, egyesített rosbridge-kapcsolat.** A `control_panel.html` a `dashboard.html`-ből mostantól közvetlenül tartalmazza a kamera-/térkép-kezelő JS-t (C70 MJPEG+ROS-tartalék, hátsó RGB, hátsó mélység canvas-renderelés, `/map` SLAM-canvas + "↺ újra" reset), egyetlen közös `const ros = new ROSLIB.Ros(...)` kapcsolattal (korábban a `control_panel.html` és az iframe-elt `dashboard.html` **külön-külön** nyitott saját websocket-kapcsolatot ugyanahhoz a rosbridge-hez — ez most egyre csökkent). A `dashboard.html` fájl önmagában, változatlanul megmarad (lásd lent, miért).
- **Elrendezés a vázlat + a felhasználó pontosító üzenetei alapján, 3 oszlopos `.stage`:**
  1. **Bal oszlop (`.thumb-col`):** felül egy görgethető, függőleges kis-kép sáv (`.thumb-strip`) — MIND A 4 szenzor (C70, térkép, hátsó RGB, hátsó mélység) kicsiben, egymás alatt, úgy méretezve (`flex:0 0 28.6%` minden csempén, ~1/3,5), hogy pontosan **3,5 kép** férjen el görgetés előtt — ez jelzi vizuálisan, hogy lehet lejjebb görgetni. Alatta, a sávban rögzítve (nem görgethető, mindig a bal alsó sarokban): a parancsnapló.
  2. **Középső oszlop (`.big-col`):** egyetlen nagy, fókuszban lévő kép — alapból a C70 (a vázlat "(C70 alapból)" jegyzete szerint) —, amit a bal oldali kis képre kattintva lehet cserélni. A csempénkénti vezérlők (térkép "↺ újra" gomb, RGB/mélység be-ki kapcsoló) csak akkor látszanak a fejlécben, ha épp az adott szenzor van nagyban (`data-sensor` attribútum + CSS).
  3. **Jobb oszlop (`.side-col`):** a vázlat piros jegyzetei szerinti sorrendben — telemetria, szabályzó csúszkák, kerekek kézi gombjai, kar+gripper vezérlés —, de KÉT részre bontva: `.side-fixed` (telemetria + csúszkák) **mindig látszik, görgetés nélkül**; `.side-scroll` (kerekek kézi gombjai + kar/gripper) csak görgetéssel érhető el. Legalul, a `.side-scroll`-tól különválasztva, rögzítve (nem görgethető, mindig a jobb alsó sarokban): az ÉLESÍTÉS gomb + állapot-felirat (korábban a fejlécben volt, a felhasználó kérésére került ide).
  - Élő DOM-méréssel (nem csak vizuálisan) ellenőrizve 1280×720-as nézetnél: a bal sávban 3,46 kép fér el görgetés előtt (cél: 3,5 ✓), a jobb oldali görgethető rész ténylegesen túlcsordul (476px tartalom / 224px látható terület — görgetés kell ✓), a napló és az ÉLESÍTÉS gomb a nézet aljához van rögzítve (`bottom: 720px` = a viewport alja, mindkettőnél ✓).
- **Kis-kép + nagy-kép egyszerre élő.** Mivel a vázlat szerint a bal sávban MINDEN szenzor kicsiben látszik, a nagy nézet pedig külön, egyszerre — ez **két külön DOM-elemet** igényel szenzoronként (kis kép/canvas + nagy kép/canvas), nem az egy-elem-vándorlás trükköt, amit egy korábbi köztes verzióban próbáltunk. A JS mindkettőt egyszerre frissíti (pl. `c70Canvases.forEach(...)`, `mapCanvases.forEach(...)`, `[rgbImg, rgbImgThumb].forEach(...)`) — ez duplázza a képadat-feldolgozást (és az MJPEG-nél a hálózati kérést is), de helyi LAN-on ez elhanyagolható, és így tényleg minden szenzor mindig élő, akkor is, ha épp nincs kiválasztva nagyban.
- **`dashboard.html` linkje megmaradt a fejlécben, de átnevezve** ("↗ kameraképek (külön oldal)", korábban "↗ műszaki szenzornézet") — a felhasználó szerint a `dashboard.html`-nek a jövőben **csak** a kamera-/szenzorképeket kellene mutatnia, semmi mást (a telemetria/vezérlés stb. már mind a `control_panel.html`-ben van). **A `dashboard.html` fájl maga MÉG NINCS leegyszerűsítve erre** — ez egy külön, még el nem kezdett következő lépés, a felhasználó kétkedve is nyilatkozott a hasznosságáról ("sok értelmét nem nagyon látom").
- **Új interaktivitás, amit a felhasználó a kérdésemre kifejezetten jóváhagyott:** a kis képre kattintás cseréli a nagy nézetet (`layoutCamSlots`/`setBigSensor` — korábbi névvel, végleges névvel `setBigSensor(target)`). Ez ÚJ JS-logika volt (nem meglévő funkció átrendezése), de a felhasználó a tisztázó kérdésre igennel válaszolt rá, tehát szándékosan van bent.
- **Validálva és élőben tesztelve:** `node --check` a kinyert `<script>` blokkon (34 KB), CRLF-ellenőrzés (a `Write` eszköz LF-fel írt, ezért Python-nal kellett CRLF-re konvertálni commit előtt — erre figyelni kell, ha legközelebb is `Write`-tal íródik újra a teljes fájl), `<div>` nyitó/záró egyezés (69/69), egyedi ID-k ellenőrzése (62 db, nincs duplikátum). Élőben (saját beépített böngészőmmel, a felhasználó gépén, `127.0.0.1:8902`-n) kipróbálva: a kis kép → nagy kép váltás kattintásra működik, a térkép "↺ újra" gomb csak térkép-nézetben látszik, a JS console-ban nincs hiba (csak a várt `ERR_BLOCKED_BY_CLIENT`/websocket-hiba a robot LAN-címére, mert a böngésző-teszt maga nem éri el a robot hálózatát).
- **Átmeneti rémület, MÁSODIK eset ugyanabból a mintából, de ezúttal a `device_stage_files`-ben, nem a böngészőben:** a commit után közvetlenül lekért `device_stage_files` kétszer egymás után egy régi (31 563 bájtos, a mai munkamenet előtti) tartalmat adott vissza, friss (kb. 1 perces) mtime-mal. Egy újabb commit + azonnali újra-stage viszont már a helyes, friss (58 306 bájtos) tartalmat hozta vissza, stabilan, kétszer ellenőrizve. **Tanulság: nemcsak a beépített böngésző-eszköz, hanem a `device_stage_files` (device-bridge) is mutathat átmenetileg elavult tartalmat közvetlenül commit után** — ha ez történik, ne essünk pánikba (fájl-visszaállás/GitHub Desktop-gyanú), hanem próbáljunk egy újabb commit+stage kört; ha az már friss tartalmat ad, ez ugyanaz az ártalmatlan, egyszeri gikszer, mint a korábbi böngésző-cache eset volt.
- **Nyitva maradt, a felhasználóval még tisztázandó/elvégzendő:**
  1. ~~A `dashboard.html` leegyszerűsítése "csak kamera-/szenzorképek" nézetté~~ — **okafogyottá vált**: a felhasználó úgy döntött, hogy ehelyett minden szenzort (IR, LiDAR felülnézet, 3D pontfelhő is) a `control_panel.html`-be kell átemelni, majd a `dashboard.html`-t törölni. Lásd lent.
  2. Élő, a felhasználó saját gépén/böngészőjében történő visszaigazolás erre a teljes új elrendezésre (kis-kép sáv, nagy nézet váltás, rögzített napló/ÉLESÍTÉS, görgethető jobb oldalsáv) — még nem történt meg, csak az én saját böngésző-tesztem (ami a robot LAN-ját nem éri el, tehát élő kamera-/telemetria-adatot nem tud ellenőrizni, csak az elrendezést és a JS-hibátlanságot).
  3. A `docs/` mappa átnézése/rendezése (korábbról elnapolt altéma) — továbbra sem kezdődött el.

## Kar- és grippervezérlés műszaki lezárása (2026-09-23)

- A robotot csak olvasó jelleggel vizsgáltuk; `/arm_cmd` parancs nem ment ki.
- A gyári `stepper_arm` forrásból igazolva: a gripper skálája `0..100`, ahol `0 = nyitva`, `100 = zárva`, a kézi lépés `5`.
- A driver visszajelző csomagja nem tartalmaz mért karpozíciót vagy szervóhibát. A `/joint_states` karértékei nem valódi mérések.
- Egy gripperparancs is együtt küldi a három ízületi célt, ezért a gripper külön sem tehető biztonságosan élővé ismeretlen karpozíció mellett.
- Az abszolút szögcsúszkák kikerültek. A webes panel talp-balra/jobbra, karvég-előre/hátra/fel/le és gripper-nyit/zár gombos szimulációt használ, a gyári kis lépésekkel és munkatérkorlátokkal.
- Élő `/arm_cmd` publisher továbbra sincs. A daráló/sípoló ízület mechanikai átvizsgálása és egy igazolt alaphelyzet nélkül ne kapcsold be.
- Részletes átadás: [13-kar-es-gripper-vezerles.md](13-kar-es-gripper-vezerles.md).

## Robotoldali szenzorindítás kivizsgálása (2026-09-23)

- A tiszta boot utáni állapotot élőben megmértük: az `/odom` és az Astra mélység működött, a `/scan`, `/map`, `/usb_cam/image_raw` és `/camera/rgb/image_raw` nem adott friss üzenetet.
- A LiDAR, a C70 és a gmapping root folyamatait Docker indítja (`pickerbot-lidar`, `pickerbot-c70`, `pickerbot-slam`), nem udev vagy külön roslaunch. A konténerek a valódi ROS master előtt indultak. Sorrendi újraindításuk után a LiDAR kb. 12 Hz-cel, a C70 kb. 30 Hz-cel, a térkép kb. 0,66 Hz átlaggal újra megjelent.
- A `pickerbot-camera` naplója külön indulási versenyt igazolt: saját ROS mastert próbált indítani, majd eltérő `run_id` miatt összeütközött a bringuppal. A repó service-fájlja most már indulás előtt megvárja a `/run_id` paramétert.
- Az Astra S (`2bc5:0402`) és a C70 (`0bda:3031`) jelenleg ugyanazon a 480 Mbit/s-os USB 2.0 buszon van. A felhasználó megerősítette, hogy a tünet azután változott meg, hogy egy laza csatlakozó miatt az egyik USB-kábelt másik aljzatba tette. A C70 eddig nyers YUYV 640×480/30 fps módot használt; hardvere ugyanilyen felbontáson 30 fps MJPEG-et is támogat. A repó C70-indítója ezért MJPEG-re váltott.
- Elkészült a `pickerbot-sensors-recover` oneshot systemd-egység és szkript. Ez master után, sorrendben ellenőrzi az Astrát, LiDAR-t, C70-et, helyi videórelét és gmappinget; a C70-t csak az Astra után indítja. A szkript és a service-fájl szintaktikai/systemd ellenőrzése a robot `/tmp` mappájában sikeres (`systemd-analyze verify`: 0).
- A felhasználó jóváhagyása után a javítás felkerült a robotra. A C70 új, külön tagelt `pickerbot/c70:noetic-20260923-mjpeg` image-ből fut. A `pickerbot-camera` és `pickerbot-sensors-recover` systemd-egység telepítve és engedélyezve van. Célzott Astra USB-resetre nem volt szükség.
- **Tiszta rebootpróba sikeres:** a robot magától visszaállította mind az öt systemd-szolgáltatást és mind a négy aktív Docker-konténert. Friss üzenetet mértünk az `/odom`, `/camera/depth/image_raw`, `/camera/rgb/image_raw`, `/usb_cam/image_raw`, `/scan` és `/map` témán. A szenzor-helyreállító naplója `Astra RGB stream is healthy` és `Pickerbot sensor startup check completed` eredménnyel zárt.
- A bemutatóoldal C70 elcsúszásának oka két egyszerre látható médiaelem volt az MJPEG/ROS fallback átváltásakor. A kép és canvas most abszolút egymásra fed, a későn érkező ROS-képkocka MJPEG mellett figyelmen kívül marad, és egyszerre pontosan egy C70 MJPEG-kérés fut. A C70 és az Astra RGB `object-fit:contain` móddal teljes 4:3 képkockát mutat. A hátsó RGB kis- és nagy nézete élő böngészőben ellenőrizve működik.
- A helyi 8080-as SSH-alagút a robot rebootja után megszűnt. A `start-demo-view.ps1` Windows PowerShell 5.1 alatt nem találta a README ékezetes `Sudo jelszó` mezőjét kódlap-eltérés miatt. A keresés most az ASCII `Sudo` mezőkezdetet használja; az indító újra felépítette az alagutat, és mindkét snapshot URL HTTP 200 választ adott.

## Finomhangolás + minden szenzor átemelve a control_panel.html-be, dashboard.html törölve (2026-09-24)

- **Duplikált kép-/adatlekérés kijavítva.** A felhasználó észrevette: amikor egy kis kép nagy nézetté válik, a régi logika a kis- ÉS a nagy-elemet is egyszerre frissítette (MJPEG-nél dupla hálózati kérés, canvas-oknál dupla rajzolás). Új `liveTargets(sensorKey, bigEl, thumbEl)` segédfüggvény: ha az adott szenzor épp a nagy nézet (`activeBig === sensorKey`), csak a nagy elemet adja vissza frissítésre, a kis képet nem — helyette a kis csempén egy CSS-es helyettesítő szöveg jelenik meg ("nagyban látható", `.thumb-tile.active .thumb-active-note`). Minden korábbi külön `c70Canvases`/`c70Imgs`/`rgbImgs`/`mapCanvases`/`dCanvases` tömb megszűnt, helyettük mindenhol `liveTargets('tileX', bigEl, thumbEl).forEach(...)`. A C70 és a hátsó RGB MJPEG-nél explicit felfüggesztés/folytatás is van (`refreshC70ThumbFeed`/`refreshRgbThumbFeed`: váltáskor a kis kép `src`-jét eltávolítja, majd vissza-váltáskor friss (cache-bustolt) `src`-t ad neki) — így a nagy nézetben lévő szenzor kis képe ténylegesen nem kér adatot a robottól.
- **Napló nagyobb, cím nélkül.** A `.log-wrap` magassága 108px → 170px, a panel fejléce ("Parancsnapló" felirat) eltávolítva — a felhasználó szerint egyértelmű funkció felirat nélkül is, ez helyet szabadít fel.
- **Fejléc egyszerűsítve.** A `dashboard.html`-re mutató link teljesen kikerült a fejlécből (a felhasználónak másik ötlete van rá, egyelőre nincs kifejtve).
- **Kar-panel szövege eltávolítva.** A "Csak naplóz, nincs valós ROS-parancs (a gripper konvenciója nincs kalibrálva — docs/08-kezi-vezerles.md)" magyarázó szöveg kikerült a kar-panelből, a felhasználó kifejezett kérésére.
- **A `dashboard.html` MINDEN szenzora átkerült a `control_panel.html`-be.** A felhasználó két képernyőképet küldött a `dashboard.html` INFRA (IR), LIDAR — FELÜLNÉZET és 3D POINT CLOUD paneljeiről, és megerősítette: ezeknek is a bal oldali kis-kép sávban és a nagy nézetben kell szerepelniük, mert a cél a két oldal teljes összevonása. Új csempék: `tileIr`/`bigIr` (IR-kép, jelenleg placeholder-szöveggel, mivel a robot nem küld külön IR-topicot — ez a `dashboard.html`-ből változatlanul átvett viselkedés), `tileLidar`/`bigLidar` (2D LiDAR felülnézet, `/scan` topic, `drawScanOn(canvas, msg)` — a korábbi egy-canvas logika refaktorálva, hogy kis- és nagy-canvasra is tudjon rajzolni), `tilePc3d`/`bigPc3d` (3D pontfelhő, Three.js 0.128.0 + OrbitControls, `/camera/depth/points` PointCloud2 + `/scan` közös jelenetben, a `dashboard.html` `laserToCamera` TF-konstansával).
  - **3D pontfelhő: egyetlen megosztott Three.js-példány, nem duplikált renderelés.** Mivel egy teljes WebGL renderer/scene GPU-terhelését nem éri meg megduplázni kis+nagy nézetre, a `pc3dEl` konténer (a `renderer.domElement`-tel benne) fizikailag át van szülőzve (`appendChild`) a `#pc3dThumbSlot` és `#pc3dBigSlot` között, aszerint, hogy melyik aktív (`movePc3d(toBig)`), és utána `resizePc3dRenderer()` fut (mert a szülőváltás nem vált ki `resize` eseményt).
  - **`setBigSensor` bővítve:** új `bigWrapId`/`bigLabelText`/`bigPillText` bejegyzések mind a 7 szenzorhoz (C70, térkép, hátsó RGB, hátsó mélység, IR, LiDAR, 3D pontfelhő), és váltáskor a megfelelő `refreshC70ThumbFeed`/`refreshRgbThumbFeed`/`movePc3d` hívás fut le.
  - **Időzítési csapda elkerülve:** a kezdeti `setBigSensor('tileC70')` hívás a script legvégére került (a korábbi, korai helyén még nem léteztek a később deklarált konstansok, amikre az új felfüggesztés/folytatás logika hivatkozik).
- **`dashboard.html` törölve.** Miután élőben (saját böngésző-teszttel, a robot LAN-ja nélkül, tehát csak JS-hibátlanság és elrendezés szintjén) megerősítettem, hogy mindhárom új szenzor hiba nélkül működik a `control_panel.html`-ben, a felhasználó jóváhagyta a `dashboard.html` törlését ("ha sikeresen hozzáadtad akkor felőlem a dashboardot lehet is törölni"). A fájl törölve, és minden rá mutató hivatkozás frissítve: `scripts/start-demo-view.ps1` (a `-DashboardOnly` kapcsoló megszűnt, mindig `control_panel.html`-t nyit), `README.md` (a "Két fejlesztési felület" szakasz "Egyetlen fejlesztési felület"-re cserélve), `.ai-context.md` (a fájlfelsorolás frissítve).
- **Validálva:** `node --check` a kinyert `<script>` blokkon (44 KB), `<div>` nyitó/záró egyezés (77/77), egyedi ID-k ellenőrzése (nincs duplikátum), CRLF megtartva. Commit: 72 115 bájt, két egymást követő `device_stage_files`-ellenőrzéssel stabilan megerősítve.
- **Átmeneti rémület, HARMADIK eset ugyanabból a mintából:** a rev17 commit után az első `device_stage_files` egy régi, 31 913 bájtos tartalmat adott vissza — ezúttal a MÁSODIK stage-ellenőrzés is ugyanazt a régi tartalmat hozta (nem oldódott meg magától, ellentétben a korábbi esetekkel). Egy újabb commit + azonnali újra-stage viszont megint a helyes, friss (72 115 bájtos) tartalmat adta vissza, stabilan. **Tanulság megerősítve: egyetlen commit utáni elavult olvasás sosem ok a pánikra — mindig próbáljunk egy újabb commit+stage kört, mielőtt bármi mást feltételeznénk.**
- **Nyitva maradt, a felhasználóval még tisztázandó/elvégzendő:**
  1. ~~Élő, a felhasználó saját gépén/böngészőjében történő visszaigazolás~~ — a felhasználó élőben kipróbálta, két konkrét hibát talált, lásd lent (javítva).
  2. A `docs/` mappa átnézése/rendezése (korábbról elnapolt altéma) — továbbra sem kezdődött el.

## Élő visszajelzés alapján 2 valódi hiba javítva + infósáv áthelyezve (2026-09-24, második kör)

- **A `start-demo-view.ps1` PowerShell-parse hibát dobott** ("Unexpected token", "missing Catch/Finally") és a magyar ékezetek olvashatatlanok voltak a konzolon. Ok: a fájlnak nem volt UTF-8 BOM-ja; a Windows PowerShell 5.1 (`powershell.exe`) BOM nélkül a rendszer kódlapján (nem UTF-8-ként) olvassa be a fájlt, ami szétszabdalta a többbájtos UTF-8 szekvenciákat és a string-lezáró idézőjelet is félreértelmezte. Javítás: UTF-8 BOM hozzáadva a fájl elejéhez. **A `stop-demo-view.ps1` "Access is denied" hibája (python folyamat leállításakor) külön, OS-szintű probléma (valószínűleg jogosultsági eltérés) — ez NINCS javítva, a felhasználónak kell megnéznie a saját gépén.**
- **A felhasználó élőben kipróbálta a `control_panel.html`-t, és 2 konkrét hibát írt le + 2 screenshotot küldött:** a hátsó RGB és a mélység kisképe sehol nem jelent meg; térképre váltáskor a C70 kicsiben félbevágva jelent meg, a térkép pedig nagyban nem, helyette "csak a lidar felhő" látszott. A `mcp__remote-devices__Claude_Browser__*` eszközökkel (a felhasználó gépén futó, de a robot LAN-ját el nem érő beépített böngészővel) élőben megvizsgáltam a DOM-ot és a számított CSS-t — ez két, egymástól független, valódi kódhibát fedett fel (nem hálózati/robot-probléma):
  1. **`[hidden]` attribútum felülírva.** A `.big-sensor img, .big-sensor canvas { display:block; }` (és a `.thumb-tile` megfelelője) mindig felülírta a böngésző alap `[hidden]{display:none}` szabályát (az író CSS mindig nyer a UA-stílus felett), így egy még adatra váró, üres canvas `hidden`-nel is teljes helyet foglalt a flex-elrendezésben. Ez szorította az apró sarokba a "Várakozás a /map adatra." feliratot, és ez hasította ketté a C70 kisképet (kép + láthatatlan, de helyet foglaló canvas). Javítás: globális `[hidden] { display:none !important; }` szabály a `<style>` elején — ez mindig érvényesül, bármi áll utána a lapon.
  2. **C70 MJPEG-visszaállási verseny.** A `startC70Fallback()` csak első hívásra rejti el az `<img>`-et (`if (c70FallbackStarted) return;` őr), utána a periodikus (5 mp-es) újrapróbálkozás mindig ELŐRE láthatóvá tette az `<img>`-et, mielőtt tudta volna, hogy a retry sikerül-e — ha nem sikerült (mert nincs MJPEG-kapcsolat), semmi nem rejtette el újra (az `error` handler `startC70Fallback()`-ja az őr miatt effektíve nem csinált semmit). Így az első sikertelen retry után az `<img>` örökre látható maradt, és megosztozott a helyen az aktívan rajzoló canvas-szal — ez okozta a "C70 kicsiben félbevágva" jelenséget, és valószínűleg hozzájárult ahhoz is, hogy a nagy nézetben sem jelent meg helyesen semmi. Javítás: a periodikus retry már nem teszi előre láthatóvá az `<img>`-et — csak a tényleges sikeres `load` esemény (ami már eddig is helyesen kezelte a váltást).
  - Mindkét hibát élőben, szintetikus (kitalált) `/map`, `/scan` és C70-kép üzenetekkel tesztelve megerősítettem a javítás után: a canvas-ok teljes szélességet kapnak, a `<img>` a retry-k után is rejtve marad, a `pointsGeom`/`lidarGeom` helyesen töltődik fel, a 3D nézet (Three.js) renderel (tengelyek látszanak).
- **Új felhasználói kérés menet közben: az infósáv (kép neve + topic + a vezérlőgombok) a nagy kép ALJÁRA kerüljön, ne a tetejére.** A `#bigHead` (`panel-head`) a `#bigBody` UTÁN került a DOM-ba (a `.big-panel` flex-column sorrendje adja a vizuális sorrendet, nem kellett `order` CSS), a `.map-meta` (`#mapStatus`) pedig utána maradt (a `#bigHead[data-sensor="tileMap"] ~ .map-meta` CSS-szelektor emiatt továbbra is működik, mivel a `~` csak a KÖVETKEZŐ testvérekre illik). A `#bigHead` border-bottom helyett border-top-ot kapott. Élőben, DOM-méréssel megerősítve: az infósáv a viewport aljához igazodik (`y ≈ 867px` egy 900px magas nézetben).
- **Validálva és élőben tesztelve mindkét körben:** `node --check`, `<div>` 77/77, egyedi ID-k (nincs duplikátum), CRLF megtartva. Commitok: 72 888 bájt (első kör: `[hidden]` fix + infósáv áthelyezés), majd 73 316 bájt (második kör: C70-retry fix) — mindkettő két egymást követő `device_stage_files`-ellenőrzéssel stabilan megerősítve (az első körben megint jelentkezett az ismert átmeneti elavult-olvasás minta, újra-commit+stage megoldotta).
- **Amit NEM tudtam ellenőrizni:** a `mcp__remote-devices__Claude_Browser__*` (a felhasználó gépén futó beépített böngésző) nem éri el a robot LAN-ját (`ws://192.168.123.50:9090` → `ERR_BLOCKED_BY_CLIENT`/websocket-hiba) — ez egy korábbról ismert, dokumentált korlátozás, nem ennek a munkamenetnek a hibája. Emiatt a tényleges élő szenzoradat-megjelenítést (van-e kép a robot kameráiból, LiDAR-jából, mélységkamerájából) és a kézi vezérlést (billentyűzet, kerekek, ÉLESÍTÉS) csak a felhasználó tudja igazolni a saját, robot-hálózatot elérő böngészőjével.
- **Nyitva maradt:**
  1. A felhasználónak élőben (valódi robot-kapcsolattal) meg kell néznie, hogy MOST már minden szenzor (C70, térkép, hátsó RGB, mélység, LiDAR, 3D pontfelhő) helyesen jelenik-e meg, és ki kell próbálnia a maradék vezérlést (webes gombok, billentyűzet, ÉLESÍTÉS) — ez alapján fog visszajelzést adni, amire válaszul javaslatokat kell adnom a roboton továbbfejlesztendő/javítandó dolgokról.
  2. A `stop-demo-view.ps1` "Access is denied" hibája — a felhasználónak kell megnéznie a saját gépén (rendszergazdai jogosultság-eltérés gyanús).
  3. A `docs/` mappa átnézése/rendezése (korábbról elnapolt altéma) — továbbra sem kezdődött el.
